# player-4dir — 코드 리뷰 이슈

- **브랜치:** feat/player-4dir
- **리뷰 커밋:** BASE `0c42c2c` → HEAD `904e11e`(최신, 재리뷰) · 1차 `0c42c2c` → `142b5a3` (superpowers:requesting-code-review, general-purpose subagent, 2026-07-25)
- **정본:** [계획](../development/sessions/2026-07-24-player-4dir-plan.md), [QA](player-4dir-test.md)

## 판정

**머지 가능(minor 1건 수정 조건).** Critical 0 · Important 0 · Minor 2.

리뷰어가 독립 검증한 것: 피처 테스트 10/10 + 전체 605/605 통과, `tsconfig.tests.json` 타입체크 exit 0, `FacingLogic.ts`에 `cc` import 없음(ADR 002 준수), 범위 내 `.meta` 0개. facing이 변위가 아니라 입력 의도에서 나오는 것, 프레임 교체가 방향 변화 프레임에만 일어나는 것, 대각선 동률 `>=` 경계, `_frameFor` switch의 컴파일타임 소진성(strict union narrowing), 피해 박스 30→44가 두 소비처(`EnemyController`·`EnemyProjectile`)의 가정을 깨지 않는 것까지 코드로 확인.

## 이슈

### Minor 1 — `PlayerHitbox.test.ts:17` 주석이 낡음 → **수정됨(2026-07-25)**

주석이 `player.json`을 `36×60 → 반높이 30`으로 적어 뒀는데, 이번 슬라이스가 그 값을 44로 바꿔 주석이 어긋났다. CLAUDE.md의 "같은 설명이 코드·테스트·문서에 복사돼 있으면 함께 고친다" 규칙에 걸린다. **`PH=30` 상수는 기하 검증 픽스처(경계 기대값이 30 기준으로 계산됨)라 바꾸지 않고**, 주석만 "이 값은 고정 픽스처이며 player.json 라이브 값이 아니다(실제는 18/44)"로 고쳤다. 영향 없음(주석 전용), 해당 테스트 재실행 통과 확인.

### Minor 2 — 미연결 슬롯 가드가 조용한 실패 하나를 다른 것으로 바꿈 → **의도된 트레이드오프(수정 안 함)**

`_applyFacingFrame`이 프레임 null이면 대입을 건너뛰어 직전 그림을 유지한다. 리뷰어도 "결함이 아니라 트레이드오프 확인용"이라 명시했다. null 대입이 캐릭터를 투명하게 만드는 것보다 stale 프레임 유지가 덜 오해되는 실패이고, 이름별 슬롯 + QA 체크리스트 5번(네 방향 각각 확인)이 미연결을 이미 잡는다. 의도가 "stale 프레임 > 투명" 맞으므로 유지.

## Recommendation (다음 슬라이스로)

리뷰어 메모: `hurtboxHalfWidth`는 18로 유지돼 박스 **폭** 커버리지가 브릿지 72%(50px 폭)에서 50%(72px 폭)로 내려갔다. QA 문서가 이를 의도적으로 다뤘으나(몸통 코어·팔 포함 53%), **최종 아트 재튜닝 시 반높이만이 아니라 반너비도 실제 실루엣에서 다시 뽑을 것**. F62에 반영.

---

## 재리뷰 — 리워크(발치 오프셋) 분

- **리뷰 커밋:** BASE `214e40c` → HEAD `904e11e` (superpowers:requesting-code-review, general-purpose subagent, 2026-07-25)
- **판정:** **머지 가능(수정 조건).** Critical 0 · Important 2 · Minor 7.

리뷰어가 독립 검증한 것: `footprintOffsetY` 호출부가 하나뿐이라는 것, `collisionRadius`와 적 이동 해소가 무변경이라는 것, 씬의 실제 값(72×96·앵커 0.5·CUSTOM·Trim 켬)이 JSDoc이 내건 규약과 맞아 반높이 48이 정말로 원점→발바닥 거리라는 것, `resolveCircleMove`의 다섯 가드 분기가 **모두** 발밑 좌표를 돌려주므로 되돌리기 한 번이 전 분기에 옳다는 것, 부동소수 왕복 오차가 프레임당 약 1e-13px로 편향 없이 상쇄된다는 것, 그리고 장애물 간격 200px·아레나 4800px 실측으로 "두 제약이 같은 프레임에 안 걸린다"는 기존 주석이 여전히 참이라는 것.

### Important 1 — 아레나 경계 수동 항목이 검증 불가능 → **수정됨(2026-07-25)**

"경계에서 멈추는 위치가 리워크 전과 같다"는 항목은 경계에 그려진 벽이 없고 카메라도 같은 사각으로 클램프돼 **비교 기준이 화면에 없다.** 게다가 이 항목이 잡으려던 실패(클램프를 발밑 좌표에서 해서 아레나가 23px 밀리는 것)야말로 눈으로 볼 수 없는 것이다. 그대로 두면 테스트 없이 체크만 되는 항목이 된다. 항목을 관측 가능한 것("경계까지 갔다가 되돌아 나올 수 있다 · 붙었을 때 떨리지 않는다")으로 바꾸고, 좌표계 검증은 아래 Important 2의 자동 테스트로 옮겼다.

### Important 2 — 실제 위험이 있는 좌표계 합성이 순수 테스트로 닿지 않음 → **수정됨(2026-07-25)**

새 테스트 5개는 스칼라 함수 하나만 덮었고, 정작 깨질 수 있는 것은 `_move` 안의 합성(발밑으로 내려 풀기 → 원점으로 되돌리기 → 원점 기준 클램프)이었다. 세 실패 모드 중 둘은 시끄럽지만(가라앉음·한 프레임 튐), **클램프를 발밑에서 하는 실패는 조용하다** — 아레나 경계가 통째로 밀려도 화면에 표시가 없다. 리뷰어가 "순수 함수로 뽑든지, 안 뽑으면 명시적으로 그렇게 말하라"고 선택지를 줬고, 뽑는 쪽을 택했다: `FootprintLogic.resolveMoveAtFootprint(from, to, radius, halfHeight, obstacles, arena)`가 순서를 들고 `_move`는 배선만 한다(ADR 002). 합성 테스트 5개 추가 — 왕복 상쇄·발바닥 정지 위치·**클램프의 원점 기준**·오프셋 0 캐릭터 동일성·아레나 크기 0 무클램프.

### Minor 3 — 테스트 주석이 단언과 불일치 → **수정됨(2026-07-25)**

"기사(120px)와 마법사(96px)" 라고 적었지만 실제 단언은 반높이 60(기사)과 32(브릿지 50×64)였다. 96px 케이스는 첫 테스트가 덮는다. 주석을 단언에 맞췄다.

### Minor 4 — 규약 목록이 세 벌로 갈라짐 → **수정됨(2026-07-25)**

`FootprintLogic` JSDoc은 규약을 셋(앵커·Trim·직립)으로, 테스트는 넷(+ **발이 그림 최하단**)으로 적어 하루 만에 어긋났다. CLAUDE.md의 "복사본은 함께 고친다"에 걸린다. 리뷰어 지적대로 **빠진 네 번째가 가장 먼저 깨질 조건**이라 정본(JSDoc)에 넣고, 깨졌을 때의 증상(발밑 불꽃·그림자·긴 망토가 알파 박스 바닥을 발보다 아래로 밀어 원을 과하게 내림 → 캐릭터가 장애물 앞 허공에서 멈춤)까지 적었다. 파일 헤더의 중복 서술은 모듈 역할 설명으로 줄였다.

### Minor 5 — 반높이 0 계약에 단언이 없음 → **수정됨(2026-07-25)**

`PlayerController.onLoad`가 "UITransform 없으면 0 → 오프셋 0"을 약속하는데 그 경로를 고정하는 단언이 없었다. `footprintOffsetY(0, 25) === 0` 테스트를 추가했다.

### Minor 6 — `_halfHeight` 캐시가 Size Mode CUSTOM에 의존 → **의존을 명시하고 유지(수정 안 함)**

리뷰어도 "지금은 맞고, TRIMMED였다면 방향마다 크기가 달라 캐시 자체가 성립 안 하므로 순서를 바꾼다고 풀릴 문제가 아니다"라며 변경 불요로 분류했다. CUSTOM 의존은 `onLoad` 주석과 QA 문서 「바꾸지 않는 것」에 이미 적혀 있다.

### Minor 7 — 백로그 F64 역링크 누락 → **수정됨(2026-07-25)**

F64는 증상이 게임필이라 `backlog.md`가 주 파일이지만 실제 결정은 코드 구조(공용 규칙 승격)라 경계 항목이다. `backlog-implement.md` 머리말의 "두 파일에 걸친 항목" 목록에 역링크를 추가했다.

### Minor 8 — 호출부 객체 리터럴 할당 → **감수(수정 안 함)**

`from`/`to`를 리터럴로 만들어 넘긴다(이전엔 `pos`를 참조로 넘겼다). 플레이어 노드 하나에 프레임당 두 개이고 `resolveCircleMove`가 이미 반환 객체를 할당하므로 영향은 무시할 수준이다. 다만 이 저장소가 할당을 명시적으로 추적하므로(F36·`NO_OBSTACLES`) 침묵 대신 감수로 기록한다.

### Minor 9 — 아레나 경계에서 그림이 삐져나오는 것 → **기록 후 백로그(수정 안 함)**

클램프가 원점 기준이라 경계에서 그림이 세로 23px·가로 11px 밖으로 나온다. **리워크가 만든 것이 아니라** 그림을 64→96px로 키운 시점부터 있던 상태다(브릿지에선 7px). 리뷰어 권고대로 "어느 판정이 발을 쓰는가"를 한곳에 모으기 위해 백로그 F64에 ③번 항목으로 넣고, QA 항목에도 판정 대상이 아님을 적었다.
