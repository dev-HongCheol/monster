# 코드 리뷰 이슈 — CLAUDE.md 3층 분할 (claude-md-split)

> - **브랜치:** feat/claude-md-split
> - **리뷰 커밋:** `cb6a498`(origin/main) → `48036c1` (3차 재리뷰 기준)
> - **판정:** 1차 Critical 0 / Important 4 / Minor 8 · 2차 Critical 0 / Important 1 / Minor 3 · **3차 신규 0건 — Ready to merge**
> - **QA 문서:** [claude-md-split-test.md](claude-md-split-test.md)

리뷰어가 배달 대상 8개 커맨드의 전이·실패 경로를 전부 따라가고, 레거시 상태 파일·경로 순회·QA 게이트 누출을 직접 재현해 확인했다. 정상 경로에서 종료코드나 상태를 망가뜨리는 경로는 나오지 않았고(**Critical 0건**), 문제는 두 갈래였다 — 배달의 **실패 경로 하나**(I1)와, 이 슬라이스가 스스로 내세운 **검증 근거의 정확성**(I2~I4).

---

## Important

### I1. 문서를 읽을 수 없으면 배달이 완전 무음이고 `steps`는 스택 트레이스로 죽는다 — 수정됨

`.claude/workflow.mjs` `readStepDoc`

`files.includes(name)`으로 이름만 확인하고 곧바로 `readFileSync`를 불렀다. 이름은 디렉터리 목록에 있는데 읽을 수 없는 경우(권한, 그 자리가 디렉터리, I/O 오류)에 예외가 그대로 올라간다. 리뷰어가 `qa-setup.md`를 디렉터리로 바꿔 재현했다.

- `approve-plan` → 종료코드 0, phase는 전이, 그런데 **본문도 경고도 없다.** `deliverAfterDispatch`의 `catch {}`가 예외와 함께 진단까지 삼켰다.
- `steps qa-setup` → `emitStepDoc`이 try/catch 밖이라 `EISDIR` 원시 스택으로 죽고 종료코드 1.

**왜 문제인가.** 종료코드는 안 바뀌었으니 불변식 ②는 지켜졌지만, 이 설계가 공들여 세운 명제 — 부재는 스스로를 알리지 않으므로 존재로 신호를 보낸다 — 가 정확히 여기서 깨진다. **절차가 안 나갔는데 아무 말도 안 하는 것이 이 기구가 막으려던 단 하나의 결과다.** 게다가 `steps`는 압축 이후의 복구 커맨드라, 그게 죽으면 복구 경로 자체가 사라진다.

**수정.** `readStepDoc`에 `try/catch`를 둘러 읽기 실패를 부재와 같이 `null`로 내려보냈다. 두 경로 모두 기존 누락 경고가 이유를 말하게 된다. 누락 경고는 원인을 셋으로 나눈다 — 디렉터리 자체가 없음 / 파일만 없음 / **파일은 있으나 읽을 수 없음**. `deliverAfterDispatch`의 `catch`도 더 이상 조용하지 않게 stderr 한 줄을 남긴다(종료코드는 그대로).

### I2. QA 문서가 테스트 없는 항목에 `[x]`를 찍었다 — 수정됨

`docs/qa/claude-md-split-test.md` §4.4

"배달은 종료코드를 바꾸지 않는다 — 배달 경로에서 예외가 나도 삼킨다" 항목을 뒷받침하는 테스트가 없었다. E2E 22건 중 배달 경로에서 예외를 유발하는 것이 하나도 없었다(`omitDoc`·`omitDir`은 `null` 반환 경로라 예외가 아니다). §4 머리말이 "피처 테스트 N/N"을 통과 근거로 걸어 두었으므로, **검증되지 않은 주장이 검증된 것으로 기록됐다.** QA 문서가 코드의 거울이어야 한다는 규칙이 겨냥하는 바로 그 어긋남이고, I1에 따르면 그 주장은 절반만 참이었다.

**수정.** I1을 고친 뒤 그 경로를 실제로 덮는 테스트 셋을 넣고 항목을 그 테스트에 묶었다. 파일 자리에 디렉터리를 두어 읽기 실패를 만든다(권한 조작보다 이식성이 좋다).

- 읽기 실패 시 종료코드 0 + phase 전이 + "파일은 있으나 읽을 수 없음" 경고
- 읽기 실패에도 `steps`가 죽지 않는다
- `steps <phase>`가 상태 파일 없이도 돈다 (M2와 함께)

### I3. 규칙 인벤토리의 내부 상호참조 7건이 엉뚱한 규칙을 가리키고, A-15의 완결성 주장이 사실이 아니었다 — 수정됨

`docs/qa/claude-md-split-test.md` 부록 A

계획이 "리뷰어가 실제로 검토할 수 있는 대상은 이 표뿐"이라고 못 박은 산출물인데, 비고의 R번호가 재번호 이후 갱신되지 않았다. **`CLAUDE.md`가 「정본 문서의 절 번호를 바꾸지 않는다 — 재번호하면 참조가 조용히 어긋난다」로 설명하는 사고가 그 규칙을 옮기는 문서 안에서 났다.**

| 행 | 적힌 것 | 그 번호의 실제 내용 | 고친 값 |
|---|---|---|---|
| R4 | R40 | gbrain 색인 stale | R46 |
| R13 | R39 | 검색 라우팅 | R45 |
| R37 | R47 | 상태 변경은 wf CLI로만 | R60 |
| R76 | R124 | squash 커밋 메시지 확인 | R137 |
| R88 | R94 | 기능 단위 커밋 | R98 |
| R104 | R109~R117 | B층 user-verification 항목들 | R127~R136 |
| R107 | R126 | gbrain 색인 갱신 | R140·R141 |

**A-15도 사실이 아니었다.** "아래 넷 말고는 새로 만든 규칙이 없다"고 적었는데, `README.md`가 신규 규범 둘(절차 문서 크기 상한, 게이트 이름을 `##` 제목에 드러내기)을 도입하고 `implementation.md`에 "리팩터링 중에는 동작을 더하지 않는다"가 원문 없이 들어가 있었다. 그 상태로는 §5.1의 「인벤토리에 기록되지 않은 규칙이 새 문서에 없다」가 7단계에서 실패한다. N5·N6으로 추가하고 "여섯"으로 고쳤다.

### I4. 「문서 정리 규칙」이 "그대로"로 기록됐는데 규범 문장 둘이 잘렸다 — 수정됨

`CLAUDE.md` 「문서 정리 규칙」 (인벤토리 A-11은 R127~R136을 전량 "그대로"로 기록)

원문 대조로 확인한 누락 둘을 복원했다.

1. **"세션 문서는 「확정」을 주장하지 않는다. 그때 무엇을 정했는지를 적고, 확정은 정본이 든다."** — 세션 문서를 *어떻게 쓸 것인가*를 지시하는 독립 규범인데 새 `CLAUDE.md`·B층 어디에도 없었다.
2. **"(세션 문서 머리말의 `정본:` 줄)"** — 링크 방향 규칙에서 세션 문서가 머리말에 `정본:` 줄을 단다는 컨벤션이 사라졌다. 이 슬라이스의 계획 문서 자신이 그 컨벤션을 쓰고 있다.

부록이 "그대로"라고 말하는 항목이 실제로는 편집됐다는 뜻이라, 대조표의 신뢰도 자체에 걸리는 문제였다.

---

## Minor

| # | 지적 | 처리 |
|---|---|---|
| M1 | `status()`가 검증되지 않은 `s.phase`를 경로로 출력한다 (파일은 안 읽으므로 보안 문제는 아니고, 진단이 헛소리를 할 수 있다) | **수정됨** — `PHASES.includes` 한 줄을 태웠다 |
| M2 | `steps <phase>`가 인자를 명시해도 `load()`가 먼저 돌아 상태 파일이 없으면 죽는다. 절차를 잃었을 때 쓰는 커맨드가 상태를 잃은 상황에서 안 도는 셈 | **수정됨** — 인자가 있으면 상태를 읽지 않는다 |
| M3 | 배달이 다시 찍는 상태 줄이 원본과 어긋난다. `pass review` → `✓ pass →`(인자 누락), `invalidate` → `→ phase=`가 전이를 함의하는데 전이는 없었다 | **수정됨** — 인자를 포함한 원본 표기를 쓰고, 재배달은 `— phase=… 절차 재배달`로 문구를 나눴다 |
| M4 | `check-docs`가 `README.md` 누락을 잡지 않는다. 존재 단언이 테스트에만 있다 | **수정됨** — `check-docs`와 `findStepDocIssues` **양쪽**의 기대 목록에 넣고 fixture 테스트를 추가했다 |
| M5 | 절차 문서 합계 실측치가 틀렸다(11,663 vs 실제) | **수정됨** — 수정 후 값 11,701자로 갱신. 측정 규율을 논지로 삼은 슬라이스라 값은 맞춘다 |
| M6 | `docs_delivered`는 phase가 바뀔 때마다 비워지므로 원소가 절대 둘이 될 수 없다. 배열이 오해를 부른다 | **수정됨(주석)** — 구조는 계획대로 두고, 항상 `[]` 아니면 `[현재 phase]`라는 주석을 달았다 |
| M7 | QA 문서 규격 세부 둘이 이전 중 빠졌다 — 에디터 체크리스트의 `✅/❌`·수동 항목의 `[ ]` 형식, 그리고 "Position·Size는 **씬 좌표계 기준으로** 명시" | **수정됨** — `qa-setup.md`에 복원 |
| M8 | phase 어휘가 세 곳에 있다 (`PHASES`, `DOC_EXEMPT_PHASES`, 테스트의 `DELIVERED_PHASES`) | **수정 안 함** — `parsePhases`가 실물 게이트에서 소스를 파싱하므로 어긋나면 잡히고, `workflow.mjs`를 import 가능하게 만드는 것은 계획 §10이 스코프 밖으로 뺐다. 양쪽에 경고 주석이 있다 |

---

## 권고 처리

- **부록 A는 R번호 대신 규칙 이름으로 상호참조하라** — 이번 슬라이스에서는 번호를 고치는 데 그쳤다. 이 문서는 시점 기록이라 다음에 인벤토리를 만들 때 적용할 일이고, 지금 전면 개편하면 리뷰 대상이 더 커진다.
- **`check-docs`와 `findStepDocIssues`는 같은 커밋에서 함께 고쳐라** — M4가 그 규율의 첫 시험이었고, 실제로 양쪽을 같이 고쳤다. 두 파일 모두 서로를 가리키는 주석을 들고 있다.
- **준수율 가설의 관찰 자리를 남겨라** — 계획 §1이 "다음 두세 슬라이스에서 절차 누락이 나는지로 관찰한다"고 적었는데 그걸 추적할 자리가 없었다. `backlog-implement.md`에 **F71**로 넣었다.

---

# 재리뷰 (`36af12c` → `15478e6`)

1차 지적을 반영한 커밋을 같은 리뷰어에게 다시 걸었다. **1차 이슈는 전부 닫힌 것으로 확인됐다** — 선언을 읽고 넘긴 것이 아니라, 읽기 실패를 실제로 재현하고, 수정 코드를 되돌려 새 테스트가 RED가 되는지 확인하고, R번호 153개의 연속성과 상호참조 21건을 전수 대조했다. 남은 것은 **수정 과정에서 새로 생긴 문제 하나**와 **1차 때 덜 닫힌 것 셋**이다.

## Important

### NEW-1. 백로그 파일 전체가 CRLF로 바뀌어 한 줄 추가에 122줄 유령 diff가 붙었다 — 수정됨

`docs/development/backlog-implement.md`

F71 한 줄을 넣으려고 쓴 스크립트가 파일 전체를 다시 쓰면서 줄바꿈을 LF에서 CRLF로 바꿨다. 리뷰어가 바이트 단위로 확인한 결과 **내용은 무손실**이었다(신규 1줄, 소실 0줄, 항목 ID 재번호 0건). 문제는 순전히 diff다.

**왜 문제인가.** 레포에 `.gitattributes`가 없어 커밋된 바이트가 그대로 남는다. PR의 Files changed에서 이 파일이 122줄 변경으로 떠서 리뷰어가 그 노이즈 속에서 F71 한 줄을 찾아야 하는데, **이 슬라이스가 「문서 정리 규칙」으로 지키려는 것이 정확히 "정리 결과가 리뷰 대상 안에 보이는 것"이다.** 게다가 그 파일의 `git blame`이 122줄 전부 이 커밋을 가리키게 되어, 다음에 백로그 행의 출처를 되짚을 때 이력이 끊긴다. 백로그는 매 슬라이스 0-1단계에서 필수로 열리는 문서라 그 비용이 반복된다.

**수정.** 파일을 LF 판으로 되돌리고 행을 다시 얹었다. `git diff --numstat`이 `2 0`(추가 2 / 삭제 0)이고 CRLF 0줄임을 확인했다. 레포 전역 EOL 정책은 이 슬라이스 스코프 밖이라 **F72**로 넘겼다.

## Minor

| # | 지적 | 처리 |
|---|---|---|
| NEW-2 | 새로 넣은 `catch (e)`가 `e.message`에 직접 접근한다. `e`가 `null`로 던져지면 catch **안에서** TypeError가 나 잡아 줄 곳이 없고, 그러면 「배달은 종료코드를 바꾸지 않는다」가 깨진다. 도달 가능성은 사실상 0(`JSON.parse`·`fs.*`는 Error를 던진다)이지만, 이 catch의 존재 이유가 그 불변식이다 | **수정됨** — `String(e)` |
| RESIDUAL-1 | M4의 CLI 쪽 변경을 어떤 테스트도 잡지 않는다. `check-docs`의 README 포함만 되돌리고 헬퍼를 그대로 두면 41/41이 그대로 통과한다(리뷰어가 mutation으로 확인). 두 구현이 복사본이라는 경고가 주석과 QA 문서에만 있고 기계 장치가 한쪽에만 있었다 | **수정됨** — 샌드박스에 `omitIndex` 옵션을 더해 `check-docs`가 종료코드 1 + stderr에 `README.md`를 내는지 단언한다. 「두 곳을 함께 고친다」는 규율에 이빨이 생겼다 |
| RESIDUAL-2 | 구 `CLAUDE.md` 281개 문장을 3-gram 커버리지로 재조사한 결과, **지시문은 전부 살아 있고** 근거절·구체 참조 다섯이 인벤토리 밖에서 유실돼 있었다 | **부분 수정** — 아래 |

### RESIDUAL-2 처리

구체 참조 셋은 짧아서 그대로 복원했다(전부 `qa-setup.md`).

1. **「초기 `walking-skeleton-test.md` 수준으로」** — 조립 레시피가 얼마나 상세해야 하는지의 **유일한 기준점**이라 가장 아까웠다. 사용자 피드백에서 나온 캘리브레이션이라 이게 없으면 "충분히 구체적"을 판정할 앵커가 사라진다.
2. **목업이 `docs/decisions/*.html`에 있다** + 충돌 예시(계획 "가로 한 줄" vs 목업 3×2 그리드) — 어디를 봐야 하는지가 없으면 규칙이 실행 불가다.
3. **2026-08-08 사고의 구체형**(`player-4dir-test.md`의 `Content Size 72×96` `✅`를 `⬜`로 되돌림) — 인벤토리 R70 비고가 "사고 근거 유지"라고 적었는데 추상적 결과만 남아 표기와 실제가 어긋나 있었다.

나머지 둘(R133의 근거절)은 **복원하지 않고 기록했다** — 인벤토리 **R154**에 「삭제(압축) — 지시문은 유지, 근거절만 축약」으로 올렸다. A층 예산 14,000자 안에서 지시문 전부를 온전히 유지하려면 근거 일부를 접어야 하고, 중요한 것은 완벽한 복원이 아니라 **무엇이 왜 잘렸는지가 기록에 남는 것**이다. 그게 이 슬라이스가 인벤토리를 만든 이유다.

---

---

# 3차 (`15478e6` → `48036c1`) — 신규 이슈 없음

2차 수정을 같은 리뷰어에게 다시 걸었고 **신규 Critical·Important·Minor 0건**으로 닫혔다. 확인 방식만 남긴다.

- **NEW-1** — `origin/main..HEAD` 백로그 순 diff `2 0`, 순수 LF. 나아가 이 슬라이스가 건드린 **16개 파일 전부**를 커밋된 blob 기준으로 훑어 16/16 LF, base 대비 EOL 변경 0건, 전 범위에서 raw diff와 `--ignore-all-space` diff가 완전히 일치함을 확인했다 — **리뷰어가 보는 diff가 곧 실제 변경이다.**
- **RESIDUAL-1** — 2차 때 통과해 버렸던 mutation(`check-docs`의 README 포함만 되돌리기)을 다시 걸어 **이번엔 RED**임을 확인했다. 두 복사본의 정합에 실제로 이빨이 생겼다.
- **RESIDUAL-2** — 복원한 참조 셋이 **실재하는지까지** 확인됐다(`walking-skeleton-test.md`, `docs/decisions/hud-layout.html`·`result-stats.html`). R1~R154 중복·결번 0, 상호참조 전부 유효.
- **3-gram 재조사** — 구 `CLAUDE.md` 281문장을 신 코퍼스에 다시 대조해 저커버리지 30건을 전수 판정했다. **인벤토리 밖 규범 유실은 더 없다** — 30건은 전보체를 서술체로 바꾸며 생긴 문자열 유사도 저하이고 지시문은 모두 새 위치에 있다.

**더 쫓지 않기로 한 것 둘.** QA 문서 파일명 **예시**(`player-4dir-test.md` → `player-final-art-test.md`)와 R79의 이력 괄호("2026-08-08 이전 문서 셋에는 병기 형식이 남아 있다")다. 둘 다 살아 있는 규칙의 삽화이고 A층 예산과 직접 경쟁한다 — 여기서부터는 수익이 비용을 밑돈다는 리뷰어 판단에 따랐다.

---

## 이력

- 2026-08-08 1차(`cb6a498` → `36af12c`): Critical 0 / Important 4 / Minor 8. Important 전부와 Minor 7건 수정, M8은 스코프 밖 사유로 보류.
- 2026-08-08 2차(`36af12c` → `15478e6`): 1차 이슈 전부 닫힘 확인(재현·mutation·전수 대조). 신규 Important 1(CRLF 유령 diff) + Minor 3. 전부 처리했고 RESIDUAL-2만 부분 복원 + 기록으로 닫았다.
- 2026-08-08 3차(`15478e6` → `48036c1`): 신규 0건. **Ready to merge: Yes.**
- 2026-08-12 4차(`53be344` → `fd049f3`, 정본 기능 리워크분): Critical 1 / Important 3 / Minor 6. **Ready to merge: No — with fixes.** 아래 4차 절 참고.

---

## 4차 — 정본 생성기와 선언 게이트 (2026-08-12)

리워크로 들어온 `wf canon`·`canon-done`·`canon-skip`과 `pass`의 정본 선언 게이트를 봤다. 범위는 `53be344..fd049f3`이고 그 앞은 3차까지 닫혀 있다.

### Critical

- **C1 (수정됨). `insertCanonRow`가 파일의 첫 구분선에 행을 넣어 「분류 접두사」 표를 망가뜨렸다.** 두 README 모두 접두사 표가 목록보다 앞에 오는데, 목록이 비어 있으면 `headerIdx + 1`이 앞 표를 가리켰다. 실물 `docs/design/spec/README.md`로 재현했다 — 행이 접두사 표에 앉고 목록은 빈 채로 남는데 종료코드는 0이고 메시지는 "등재"라고 말했다. 요구사항 3(모든 정본은 인덱스에 등재된다)이 통째로 깨지는 경로이고, 하필 테스트 커버리지가 0이던 `--design` 분기였다. **「목록」 제목을 찾고 그 뒤 첫 구분선을 앵커로 삼도록 고쳤고**, 표 범위도 `|`로 시작하지 않는 줄에서 끊어 뒤따르는 표를 먹지 않게 했다. 픽스처를 실물과 같은 두 표 모양으로 바꿨다 — 표가 하나뿐인 픽스처가 이 버그를 초록불로 통과시킨 원인이다.

### Important

- **I1 (수정됨). `canon-done`이 상태 파일 없이 실행되면 raw 스택으로 죽었다.** `recordCanon`은 상태 파일이 없으면 `null`을 돌려주는데(슬라이스 밖 사용 허용), 호출부가 `s.canon_updated`를 무방비로 읽었다. 이 파일에서 `fail()`을 거치지 않고 죽는 유일한 경로였다. null이면 기록만 생략하고 정상 종료하도록 고쳤다.
- **I2 (수정됨). 레포 밖 검사가 Windows 드라이브 경계를 못 막았다.** ROOT가 `C:`일 때 `path.relative('C:\\box', 'F:\\...')`는 `../`가 아니라 절대 경로를 그대로 돌려준다. 이 프로젝트가 실제로 도는 배치(레포는 `F:`, 시스템은 `C:`)라 `fd049f3`이 막으려던 것이 그대로 샜다. `path.isAbsolute(rel)`를 조건에 더했다.
- **I3 (수정됨). CLI 복제본에 구멍이 있었다.** 리뷰가 CLI만 네 가지로 변형했는데 75개 테스트가 전부 통과했다 — `--design` 분기는 커버리지가 0(`grep -rn -- "--design" tests/` 0건)이었고 정렬·멱등·파이프 가드·템플릿 본문이 헬퍼 전용이었다. `--design` E2E 두 개를 추가하고 그 안에서 정렬까지 고정했다. 재검증: `--design` 분기 제거·정렬 제거·목록 앵커 되돌리기 세 mutation이 모두 실패한다.

### Minor

- **M1 (수정됨). 쓰기 순서.** 문서를 먼저 쓰고 인덱스에서 죽으면 등재 안 된 정본이 남는데, 재실행하면 `이미 있습니다`로 막혀 영영 등재되지 않는다. 인덱스를 먼저 쓰도록 뒤집었다 — `insertCanonRow`가 멱등이라 재실행이 안전하고, 최악이 "가리킬 대상 없는 인덱스 행"이라 눈에 보인다.
- **M2 (수정됨). `STEP_DOC_INDEX` 재사용.** 절차 문서 인덱스와 정본 인덱스는 값만 같고 다른 개념이라, 전자를 고치면 후자가 조용히 깨진다. `CANON_INDEX`를 따로 뒀다.
- **M3 (수정됨). `canon-done`이 디렉터리를 받았다.** `fs.statSync(full).isFile()`을 더했다.
- **M4 (수정됨). QA 문서 수치가 낡았다.** 4차 수정 전 기준으로 적혀 있던 것을 실측으로 맞췄다.
- **M5 (수정됨). 헤더 판정의 죽은 코드와 정렬 지정자.** `l.replace(/\s/g, ' ')`는 뒤따르는 정규식이 이미 `\s*`를 쓰므로 무의미했고, `|:---|---:|` 꼴을 인식하지 못했다. 정규식을 `^\|(?:\s*:?-+:?\s*\|)+$`로 바꾸고 죽은 치환을 뺐다.
- **M6 (보류). `implementation`에서 한 정본 선언이 `start-verification`에 지워진다.** 그 커맨드가 `resetVerification`을 부르기 때문이다. 절차 문서는 선언을 `verification`에 두고 있어 정상 경로에서는 안 걸리고, 걸려도 `canon`이 재생성을 거부하며 `canon-done`을 안내해 회복된다. phase 가드를 더하는 것은 커맨드를 슬라이스 밖에서도 쓸 수 있게 한 설계와 부딪쳐 보류하고, `verification.md`에 한 줄로 남겼다.
