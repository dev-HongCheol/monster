# 코드 리뷰 이슈 — 아트 정본 이전 (art-canon-move)

- **브랜치:** `feat/art-canon-move`
- **리뷰 범위:** `566c32a`..`77f2c2c` (독립 서브에이전트 1차)

---

## 1차 리뷰 (2026-08-14, `77f2c2c`)

### Critical

**C1. 재지정한 링크가 대상에 없는 문장을 인용했다 — 수정됨.**

`art-generation-playbook.md:879`가 `game-combat.md` §3을 가리키며 *"본체보다 크게 그린 장식은 닿아도 안 맞는데, 그건 의도한 결과이며 공정하다"*고 그 문서가 적어 뒀다고 주장했다. 실측하면 그 문장은 **ADR 007:55와 `art-direction.md`에 있고 `game-combat.md` §3에는 없다** — §3이 드는 것은 "본체 실루엣의 비율과 크기를 최대한 유지한다"와 자식 노드 규칙뿐이다.

문제는 그 문장의 논증 구조가 "정본이 이미 그렇게 적어 뒀으니 내가 어긋나지 않는다"라는 점이다. 링크를 타고 §3을 열어 확인하려던 사람은 근거를 못 찾고, `hurtboxHalfWidth`를 몸통 폭으로 잡는 결정의 버팀목이 사라진다. **계획 §9가 예고한 실패 형태 그대로다** — 링크는 풀리고 검사기는 초록인데 주장만 거짓이다.

내용 자체는 잃지 않았다. `art-direction.md` §6과 `art-asset-spec.md` §3.1이 같은 분석을 온전히 들고 있다. 그래서 링크는 판정 규칙에 두되 **인용의 출처를 `art-direction.md` §6으로** 돌렸다.

### Important

**I1. 범위 정규식에 공허 통과 가드가 없었다 — 수정됨.** `DocLinks.test.ts`의 「정본은 결정 기록을 링크하지 않는다」가 범위 밖 문서를 건너뛰는데, 정규식을 잘못 고쳐 **아무 문서도 안 고르면** `offenders`가 빈 채로 통과한다. 검사하지 않으면서 초록을 내는 상태다. 같은 구멍을 CLI 쪽에서 막으려고 세운 것이 바로 옆의 「`wf check-links`가 이 파일을 가리킨다」 단언이라, 여기만 비어 있는 것이 앞뒤가 안 맞았다.

범위를 `CANON_SCOPE` 상수로 꺼내고 **네 층이 각각 하나 이상 잡히는지**를 먼저 잰다. 리뷰가 제안한 총 개수 하한(`> 20`)을 쓰지 않은 이유는 실측 때문이다 — 범위 안 문서가 23개라 여유가 셋뿐이고, 그런 하한은 문서가 하나 늘고 줄 때마다 낡아 진짜 회귀와 평범한 증감을 구분하지 못한다. 층으로 재면 넓히기 이전 범위(13개, `planning`·`workflow` 누락)와 오타로 0건이 되는 경우 **둘 다에서 실패**하고, 실패 메시지가 어느 층이 빠졌는지 이름으로 말한다. 남는 구멍은 하나 — `CANON_SCOPE`에 다섯째 층을 더하면서 `layers` 배열에 안 더하면 그 층은 무방비다.

**I2. `CanonSpecMove.test.ts` 머리말이 형제 파일과 모순됐다 — 수정됨.** 그 파일은 스스로 "일회용 게이트다"라고 적고 있는데, 계획 §7이 `ArtCanonMove.test.ts`를 영구 회귀망으로 두는 **근거로 그 파일과의 동격을 들었다**("`CanonSpecMove.test.ts`가 지금 직전 이동에 대해 하고 있는 역할과 같다"). 둘 다 레포에 남아 같은 일을 하므로 계획 쪽이 맞다. 앞의 머리말을 고쳐 맞췄다 — 안 고치면 "슬라이스 이름이 붙은 이동 테스트를 지우지 마라"는 경고가 바로 옆에서 반박당한다.

**I3. `canon_updated` 선언이 둘 빠졌다 — 수정됨.** `CLAUDE.md`(라우팅 표 접기 + 규칙 문장 수정 + 문서 트리)와 `docs/development/spec/docs-glossary.md`(정본 안의 경로 평문 일곱 곳)가 목록에 없었다. `canon-done`으로 더했다.

### Minor

**M1. 패러프레이즈를 따옴표로 감쌌다 — 수정됨(3곳).** `art-direction.md:81`·`art-asset-spec.md:152`·`art-generation-playbook.md:244`가 `game-combat.md`의 내용을 옮기면서 원문에 없는 문장을 인용부호로 감쌌다. 뜻은 같지만 **C1을 만든 것과 같은 기제**라 따옴표를 걷고 서술로 바꿨다.

**M2. 시점 기록의 실측표를 치환이 덮었다 — 되돌림.** `sessions/2026-08-08-canon-session-link-policy.md:56~58`은 2026-08-08 기준 링크 수를 잰 표인데, 일괄 치환이 세 행의 경로를 새 경로로 바꿔 **그날 존재하지도 않던 경로에 그날의 수치**가 붙었다. 옛 경로로 되돌렸다.

**되돌리는 기준은 "코드 스팬이냐"가 아니라 "측정값이냐"다.** 다른 시점 기록의 코드 스팬 경로 넷(`sessions/2026-07-02-hud-layout-plan.md` 둘·`qa/hud-layout-test.md`·`qa/player-mage-art-test.md`)은 치환한 채로 뒀다 — 그것들은 "그 파일은 여기 있다"를 말하므로 파일이 옮겨 갔으면 새 경로가 맞다. 반면 이 표는 **그날 잰 값**이라 경로를 갱신하면 측정 자체가 거짓이 된다(새 경로 기준으로 재면 지금 0이다). 다음 이동 슬라이스도 이 기준으로 가른다.

**M3. 산문 포인터 둘이 과하게 얇아졌다 — 수정됨.** `playbook:151`의 `§5`가 링크가 사라지자 playbook 자신의 §5로 읽혔고(→ "그 문서 §5"), `:713`의 인페인팅 문단은 이웃과 달리 날짜를 잃었다(→ "2026-08-07 실측" 복원).

**M4. `CanonSpecMove.test.ts:38`의 JSDoc이 자기를 가리켰다 — 수정됨.** 설명은 `tracked`에 있는데 `findTrackedFiles`를 가리키고 있었다.

**M5. `loadDocs()` 4회 호출과 `findTrackedFiles` 3중 중복 — 백로그로.** 슬라이스가 네 번째 호출을 더했지만 구조 자체는 이전부터다. 두 항목 모두 **F82**에 붙였다.

**M6. `docs/design/mockups/`가 범위 정규식 밖 — 조치 없음.** 지금 그 폴더에 `.md`가 없어 문제가 없고, 생기면 그때 넣는다.

### 리뷰가 지적했으나 고치지 않은 것

**계획 §2-4와 §4의 기준이 서로 다르다(리뷰 #9의 후반).** 세션 문서에 대해 **경로가 붙은 참조는 치환하고 맨 파일명은 두는** 것이 이 슬라이스의 규칙인데, "다른 슬라이스의 결정 기록은 찾아가 고치지 않는다"는 원칙에서 보면 슬래시 유무로 답이 갈리는 셈이다. 이번에는 **링크가 깨지면 안 된다**는 실질이 경로 쪽을 강제하므로 그대로 두되(M2처럼 링크가 아닌 자리는 되돌렸다), 기준을 정리하는 일은 **F80**이 든다.

---

## 2차 리뷰 (2026-08-14, `0bda948` — 1차 수정분 재검토)

여덟 건 전부 **해소로 확인**됐다. 새로 나온 것은 아래 여섯이고, 그중 하나가 실질이다.

**N1. 이 문서가 코드에 없는 구현을 「수정됨」으로 적고 있었다 — 수정됨.** 위 I1 항목이 처방을 `inScope.length > 20`으로 적었는데, 그것은 **버린 첫 안**이고 실제로 들어간 것은 층별 존재 검사다. 게다가 `DocLinks.test.ts`의 주석이 두 파일 건너에서 "총 개수 하한으로 재면 안 되는 이유"를 적고 있어 서로 반박했다. **C1과 정확히 같은 결함이 C1을 기록하려고 쓴 문서에서 났다** — 기록이 대상의 내용을 잘못 주장하는 것. I1 항목을 실제 구현으로 다시 썼다.

**N2. F82 제목이 "구멍 넷"인데 항목이 다섯이었다 — 수정됨.** ⑤(도구 중복)를 더하면서 수를 안 고쳤고, ⑤는 검사기의 구멍이 아니라 중복이라 제목과도 어긋났다. 「구멍·중복 다섯」으로 고쳤다.

**N3. F84가 F82와 F83 사이에 끼어 ID 오름차순이 깨졌다 — 수정됨.** F83 뒤로 옮겼다.

**N4. 이 문서의 I2 항목이 인용 출처를 잘못 댔다 — 수정됨.** "`CanonSpecMove.test.ts`가 직전 이동에 대해 하고 있는 역할과 같다"는 `ArtCanonMove.test.ts`가 아니라 **계획 §7**의 문장이다.

**N5. M2의 되돌린 기준이 틀리게 적혀 있었다 — 수정됨.** "코드 스팬이라 고칠 이유가 없었다"고 썼는데, 다른 시점 기록의 코드 스팬 경로 넷은 치환한 채로 뒀으므로 그 기준으로는 설명이 안 된다. 실제 기준은 **측정값이냐**다 — 그 표는 그날 잰 수치라 경로를 갱신하면 측정 자체가 거짓이 된다.

**N6. `playbook:713`이 §2.3에 없는 문장을 §2.3의 것으로 인용했다 — 수정됨.** "몸통 앞을 가로지르지 않게"는 §5.4의 판정 항목(`:477`)이고 §2.3은 같은 취지를 다른 말로 지시한다. 이 슬라이스가 만든 것은 아니지만(`566c32a`에도 같았다) 날짜를 되살리며 **바로 그 줄을 고치면서 놓쳤고**, F84가 겨냥하는 바로 그 종류라 함께 닫았다.

### 리뷰 지적 중 사실이 아닌 것

**N7(절차 위반 지적)은 성립하지 않는다.** 리뷰는 상태 파일의 `cso_done`·`ts_check_clean`·`lint_clean`이 `77f2c2c` 시점 값이라 현재 트리를 설명하지 못한다고 봤다. 실제 순서는 **`invalidate` → 수정 → `pass cso` → `pass ts` → `pass lint` → 커밋**이었고, 세 플래그는 커밋된 트리에서 다시 채워진 값이다. 상태 파일만으로는 그 순서를 볼 수 없어 생긴 추정이다.

## 확정 이력

- 2026-08-14 — 1차 리뷰 기록. C1·I1·I2·I3·M1~M4를 고쳤고 M5는 F82로, 기준 정리는 F80으로 넘겼다.
- 2026-08-14 — 2차 리뷰 기록. 1차 여덟 건은 전부 해소 확인. N1·N4·N5는 **이 문서 자신의 부정확**이라 여기서 고쳤고, N2·N3은 백로그, N6은 정본 인용을 고쳤다. N7은 사실이 아니라 반박만 적었다.
