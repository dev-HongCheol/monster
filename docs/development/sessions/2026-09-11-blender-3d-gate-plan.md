<!-- /autoplan restore point: /c/Users/Choi-HC/.gstack/projects/dev-HongCheol-monster/feat-blender-3d-gate-autoplan-restore-20260911-040335.md -->
# 3D 마스터가 게임 화면까지 가는지 프레임으로 관통시킨다

- **작성일:** 2026-09-11
- **브랜치:** `feat/blender-3d-gate`
- **상태:** 계획 (승인 대기) — 외부 검토 1회 · /autoplan CEO·DX·Eng 리뷰 완료 · **리뷰 결정을 본문에 반영함**. 남은 것은 §8의 회차 수 확정과 승인이다 — 부록 B
- **정본:** [`ops-licensing.md`](../spec/ops-licensing.md) §2 · [`art-asset-spec.md`](../../design/spec/art-asset-spec.md) §9.1.1 — 이 슬라이스가 도구 행 셋과 3D 소스·판정 증거의 보관 규칙을 더한다. 플레이어 아트를 3D로 옮기는 정본 개정과 **최소 ADR 한 장**은 통과 뒤의 슬라이스가 한다(§9)
- **여는 백로그:** 신규 `F105`(§10). **`F103`·`F104`를 건너뛰는 것은 의도한 것이다**(§10.1)
- **관련 백로그:** `F60`(아트 생성 부모) · `F67`(지팡이 분리 — 이 계획이 겨누는 벽) · `F59` ①(Spine — v2로 밀림) · `F99`·`F100`
- **입력:** `docs/temp/`의 검토 셋과 외부 검토. 버전 관리 밖이라 값만 옮겨 적는다

---

## 1. 왜 하나 — 벽의 정확한 위치

**세 번의 시도가 전부 「수정하면 재생성된다」에서 막혔다.** fal.ai에서 한 방향의 손·포즈를 고치면 다른 방향까지 바뀌었고, GPT 웹으로 옮겨도 프롬프트 편집이 결국 재생성이었으며, 로컬 ComfyUI의 img2img는 선명도를 잃었다.

**다만 4방향 생성 자체는 통과했다.** 그 그림 넷이 지금 게임에 실려 있다.

> **2-a 4방향 스프라이트: 완료(2026-08-07).** 몸 4장 + 지팡이 1장이 같은 캔버스·같은 발 밑선으로 정렬돼 있고 (…) **2-b 스켈레탈:** 슬롯 분해 규약(§6)대로 파츠를 잘라 리깅한다.
> — [`art-direction.md`](../../design/spec/art-direction.md) §9

`player_4dir_*.png` 넷이 전부 246×493이고 `main.scene`이 다섯 곳에서 참조한다. **막힌 것은 2-b, 그중에서도 파츠 분리다.**

**그리고 백로그가 그 벽을 한 문장으로 적어 뒀다.**

> 지팡이가 머리카락 위를 지나므로 걷어 내면 그 뒤에 아무것도 없고(실측 머리카락 236px 소실 + 머리 왼쪽 윤곽이 세로 직선으로 잘림), 이건 실행 지침 §2.0이 "가림은 생성 시점에만 풀 수 있다"로 이미 적어 둔 종류다.
> — [`backlog.md`](../backlog.md) `F67`

**「가림은 생성 시점에만 풀 수 있다」가 문제의 전부다.** 2D 생성은 다시 돌리면 다른 그림이 나오므로 생성 시점으로 돌아갈 방법이 없다. 3D는 머리 메시를 끄면 **그 밑의 두피가 실제로 있다.** 이 슬라이스가 3D를 보는 이유가 그것이고, 다른 이유가 아니다.

**폴백이 있다는 것도 적어 둔다.** 어디서 끊기든 게임은 지금 출하된 4방향 아트를 그대로 쓴다. 되돌릴 것이 없다.

---

## 2. v1은 파츠가 아니라 프레임이다

**2026-09-11 결정. 이 슬라이스의 범위를 가장 크게 바꾸는 항목이다.**

### 2.1 파츠를 고른 이유가 3D에서 사라진다

정본이 파츠 + 스켈레탈을 고른 것은 **소스가 2D 한 장**이었기 때문이다. 한 PNG에서 걷기를 못 뽑으니 잘라서 리깅한다. **리깅된 3D는 프레임을 직접 준다** — 이 프로젝트가 잡몹에 이미 쓰는 트랙이다([`art-direction.md`](../../design/spec/art-direction.md) §3.1).

**그리고 v1이 스킨을 안 판다.**

> ### v1에서 명시적으로 제외
> - 스킨 판매, 트랜스모그
> — [`roadmap.md`](../../planning/roadmap.md) §7

슬롯 교체가 Spine 의존의 유일한 정당화인데 v1이 그것을 뺐다.

### 2.2 「지금 파츠로 만들어야 재작업이 없다」는 2D 전제였다

정본이 이렇게 적었다.

> v1은 마법사 단일·스킨 미판매지만, **파이프라인이 지금부터 슬롯 분해로 만들어져야** 나중에 스킨을 얹을 때 재작업이 없다.
> — [`art-direction.md`](../../design/spec/art-direction.md) §3.2

**그 논리는 2D를 다시 오리는 것이 비쌀 때 성립한다.** 3D에서는 같은 마스터를 파츠로 다시 렌더하는 것이 **스크립트 한 번**이다. 3D 마스터가 불변이고 파츠냐 프레임이냐는 렌더 설정일 뿐이라, **재작업 자체가 없어진다.**

```
v1   3D 마스터 ──▶ 걷기·공격 프레임 ──▶ cc.Animation
                      (잡몹과 같은 트랙)
v2   같은 마스터 ──▶ 파츠 렌더 ──▶ Spine 슬롯 교체 ──▶ 스킨 판매
     ↑ 마스터가 안 바뀌므로 v1 산출물을 버리는 것이 아니다
```

### 2.3 이 결정이 슬라이스에서 지우는 것

파츠 경계 · 어깨 겹침 여유 · 어깨 축 좌표 · Pivot 노드 구조 · Spine 구매와 버전 확인 · `F64`의 선행 승격 · `sp.Skeleton`의 Anchor·Size 무효 제약. **DX 리뷰가 찾은 치명적 둘(Trim 설정으로 축이 깨지는 것, 자식 스프라이트 위치 누락)도 축이 없어져 함께 사라진다.**

**대가 하나:** 프레임은 애니메이션 타이밍을 굳힌다. 고치려면 다시 구워야 하는데, 스크립트 렌더라 그 비용이 작다.

---

## 3. 무엇으로 만드나 — VRoid Studio

무료 리깅 베이스를 찾아 헤매는 대신 [VRoid Studio](https://vroid.com/en/studio)로 인물을 직접 만든다(2026-09-11 결정).

- **베이스 조달 리스크가 크게 준다.** 조건 셋을 다 만족하는 무료 베이스를 찾는 일에 시간 상한이 없다. 만들면 찾을 필요가 없다.
- **얼굴을 확실히 고르고 수정할 수 있다.** 눈·코·입·윤곽을 슬라이더로 조절한다. **사용자가 건 조건**이다.
- **상업 이용이 명시적으로 허용된다** ([VRoid FAQ](https://vroid.pixiv.help/hc/en-us/articles/4405813333657-Can-I-use-the-models-created-with-VRoid-Studio-Stable-Ver-for-commercial-purposes)).

**이번 게이트는 VRoid 기본 제공 요소와 직접 만든 설정만 쓴다.** 다른 제작자의 텍스처·BOOTH 헤어·외부 VRM·외부 셰이더는 조건이 따로 붙는다. **받기 전에 확인해 [`ops-licensing.md`](../spec/ops-licensing.md) §2 표에 행을 붙인다** — VRM 임포터 애드온과 **걷기 모션**도 포함이다.

**내보내기는 VRM 1.0으로 한다.** VRoid는 0.0과 1.0을 모두 내보내고 설정에서 고르는데, 기본값에 맡기면 0.0이 나올 수 있고 그러면 모션 경로가 막힌다(부록 C-2.2).

**VRoid가 새로 만드는 미지수 하나:** 특유의 애니·VTuber 룩이 있고 슬라이더로 못 벗어난다. §6이 한 장으로 본다.

**기존 2D 인물은 폐기한다(2026-09-10 결정).** 되돌릴 수 있다 — 그 파일들은 `art-source/`와 게임에 그대로 있다. **VRoid가 맨살 베이스를 공짜로 돌려준다는 것도 적어 둔다**(`F61`의 유일한 선행이었고 2D에서는 재생성 불가였다).

---

## 4. 환경을 버전으로 고정한다

**기록만으로는 부족하다. 스크립트가 단언한다.**

- `bpy.app.version`을 스크립트가 확인하고, 범위 밖이면 검증된 버전을 이름으로 말하며 실패한다. EEVEE 엔진 식별자가 Blender 4.2에서 바뀐 전례가 있다.
- **범위는 VRM 애드온이 정한다 — 4.2 이상 5.2 이하다.** 그 애드온이 5.3 이상을 미지원으로 선언했다(부록 C-2). 리타게팅을 스크립트로 하므로 위쪽을 더 조이는 애드온은 안 들인다.
- VRM 임포터는 **릴리스 태그로 고정**하고 공식 배포처에서만 받는다. **받은 zip의 SHA-256을 QA 문서에 적는다** — 태그는 재작성될 수 있어 태그 이름만으로는 같은 파일임을 보장하지 못한다.
- QA 문서에 남길 것: Blender · VRM 애드온(버전 + 해시) · VRoid Studio · **VRM 내보내기 버전(1.0)** · Cocos Creator · GPU와 드라이버.
- **VRoid 내보내기는 구조 보존을 우선한다** — 폴리곤·머티리얼·뼈 줄이기를 전부 끈다. **감축은 텍스처 해상도로만 한다**(아래 크기 상한).
- **`.vrm`을 `art-source/player/<날짜>-3d-gate/`에 커밋한다.** 커밋하지 않으면 사슬의 출발점이 이 장비에만 있다. 경로 형태는 [`art-asset-spec.md`](../../design/spec/art-asset-spec.md) §9.1.1의 `<카테고리>/<생성일>` 규약을 따른다.
- **`.vrm` 크기 상한은 50MB다.** 넘으면 텍스처 해상도를 낮춘 판을 커밋한다. git 히스토리는 영구라 넣고 나서 무겁다고 뺄 수 없고, **Git LFS는 안 쓴다** — 그 정본이 이유를 이미 든다(LFS 없는 장비에서 클론하면 `.meta`가 재생성돼 씬 참조가 깨진다).
- **`.blend`는 커밋하지 않는다.** 재현 단위는 `.vrm`과 Blender 스크립트다. 스크립트가 있으면 `.blend`는 파생물이고, 그게 §2.2가 말한 "스크립트 한 번"의 이점이다.
- **`.gitattributes`에 `*.vrm`을 binary로 선언하고 커밋한다.** 그 파일은 아직 안 들어온 확장자까지 미리 적어 두는 방침이고, git의 자동 판정이 빗나가면 줄 끝 회귀 테스트가 전체 검증을 막는다.

### 4.1 실행 계약

`tools/blender/README.md`에 blender 실행 경로·환경 변수·정확한 호출 한 줄을 적는다. 형태는 [`tools/art/judge.ts`](../../../tools/art/judge.ts)를 따른다 — 호출법을 자기 JSDoc에 들고 최소 버전을 단언하는 것이 이 레포의 표준이다.

**파일 구성은 넷이고 대칭이 아니다.**

| 파일 | 무엇 |
|---|---|
| `smoke.py` | 게이트 0a. **독립이다 — VRM 애드온도 모델도 import하지 않는다.** 공용 모듈을 거치면 §5.1의 "여기서 막히면 VRoid를 설치하지 않는다"가 조용히 깨진다 |
| `_common.py` | VRM 임포트 · 카메라 · 툰 세팅 |
| `import_vrm.py` | 게이트 0b |
| `retarget_render.py` | 게이트 0c와 게이트 2 — 모션을 입히고 프레임을 굽는다 |

**판정은 파이썬에 두지 않는다.** Blender 쪽은 굽기만 하고, 재는 것은 전부 TS다(§11.4). 그래야 판정이 Blender 버전을 안 탄다.

**`README.md`에 실패 코드 표를 만든다 — 코드 · 무엇이 안 됐나 · 다음에 뭘 하나 세 칸이다.** 최소 여섯을 든다: Blender 버전 범위 밖 · EEVEE 식별자 없음 · VRM 애드온 미등록 · `.vrm` 경로 없음이거나 파싱 실패 · 카메라 프레이밍 밖 · 출력 경로 쓰기 실패. **메시지에 실측값을 박는다**(기대 4.2~5.2, 지금 4.1.2). 본은 이 레포에 이미 있다 — [`tools/art/Postprocess.ts`](../../../tools/art/Postprocess.ts)의 예외가 수치를 담고, `.claude/typecheck.mjs`의 실패가 없는 파일 경로와 만드는 법과 지금 상태를 셋 다 찍는다.

#### 4.2 `--python-exit-code 1` 없이는 성공과 실패가 구분되지 않는다

**`blender --background --python`은 스크립트가 예외를 던져도 종료 코드 0을 낸다.** 트레이스백은 Blender의 시작 로그에 섞여 흘러간다. 그러면 하류 판정이 낡거나 없는 PNG를 읽고 아무 값이나 보고한다.

**그래서 넷을 강제한다.** `--python-exit-code 1`로 부른다. 스크립트 본문을 `try/except`로 감싸고 실패 시 `sys.exit(1)`한다. **기계가 읽는 한 줄을 찍는다** — `GATE_OK {json}` 또는 `GATE_FAIL <code> <message>`.

**넷째가 앞의 셋을 실제로 작동하게 한다.** 실행기는 그 줄을 **「마지막 줄」로 읽지 않고** stdout에서 `GATE_OK`·`GATE_FAIL`로 시작하는 마지막 줄을 찾는다 — Blender가 스크립트 뒤에 자기 종료 로그를 더 찍기 때문이다. **그리고 그런 줄이 하나도 없으면 종료 코드와 무관하게 실패로 접는다.** EEVEE가 컨텍스트를 못 잡아 Blender가 시그널로 죽으면 `--python-exit-code`도 `try/except`도 발화하지 않는데, 그게 이 절이 막으려는 조용한 실패와 정확히 같은 모양이다. [`judge.ts`](../../../tools/art/judge.ts)가 같은 사고방식으로 짜여 있다.

---

## 5. 게이트 0 — 환경이 서는가

**둘로 나눈다. 첫째는 사람 입력이 0이다.**

### 5.1 게이트 0a — Blender 스모크 (약 10분, 사람 입력 0)

기본 큐브를 EEVEE `film_transparent`로 렌더해 투명 PNG 한 장을 쓴다. **VRoid도 캐릭터도 애드온도 필요 없다.**

답하는 것: 이 장비에서 헤드리스 Blender와 파이썬과 투명 EEVEE가 도는가.

**여기서 막히면 VRoid를 설치하지 않는다.** 되돌릴 비용이 가장 작은 지점이다.

### 5.2 게이트 0b — VRM이 들어오는가

VRoid 캐릭터를 만든 뒤 돈다. VRM 임포터가 산출물을 읽고, 그 모델이 같은 카메라로 렌더되는가.

### 5.3 게이트 0c — 걷기가 이 골격에서 도는가

**VRoid가 내보내는 `.vrm`에는 골격만 있고 모션이 없다.** 계획이 이 단계를 빠뜨렸던 것을 Eng 리뷰가 잡았다(부록 A-3.9). 조사 결과와 경로 비교는 부록 C에 있다.

**모션은 [Quaternius Universal Animation Library](https://quaternius.com/packs/universalanimationlibrary.html)에서 받는다(2026-09-11 사용자 결정).** 권리를 포기한 공개(CC0)라 표기 의무도 재배포 제한도 없고, 애드온을 안 늘려 Blender 버전이 §4의 범위에 그대로 남는다.

**리타게팅은 스크립트로 한다.** 본 이름 대응표를 두고 본마다 Copy Rotation을, Hips에 Copy Location을 걸고 Visual Keying으로 굽는다. 애드온 프리셋은 실패해도 이유를 안 주지만, 대응표는 어느 본에서 틀어졌는지 이름으로 말할 수 있다.

**묻는 것 하나:** 걷기 한 벌이 이 인물의 골격에서 어긋나지 않고 도는가.

**여기서 막히면 툰 세팅에 시간을 쓰기 전에 닫는다.** 상한을 넘으면 리타게팅 애드온으로 한 번 넘어가 보고, 그때는 Blender를 5.0~5.2로 좁힌다(부록 C-2.1).

---

## 6. 게이트 1 — 화풍 가능성 한 장

**묻는 것은 「예쁜가」가 아니다.**

> VRoid·VTuber 특유의 3D 느낌을, 이 게임에서 허용할 수 있는 수준까지 **줄일 가능성이 보이는가.**

**최소 세팅.** 직교 카메라 · 2~3단 툰 셰이딩 · 외곽선 · **스페큘러 제거** · **머리카락 하이라이트 최소화** · **얼굴 그림자 단순화** · 투명 배경. 뒤의 셋이 3D 느낌을 만드는 주범이다.

### 6.1 판정 도구 — 비교 시트 한 장

**「거슬리는 것이 있나」만으로는 답할 수 없다. 볼 물건을 만든다.**

한 장짜리 대조 시트에 네 칸을 넣는다. 새 렌더 246×493 · 새 렌더 **96×192** · 출하된 `player_4dir_front.png` 246×493 · 같은 것 96×192.

**96×192는 계산값이므로 재서 확정한다.** 표시 48×96 월드 단위는 씬에서 실측으로 맞지만, 그것이 화면 몇 픽셀이 되는지는 디자인 해상도에 달렸고 **이 프로젝트는 그 값을 명시하지 않아 엔진 기본값을 쓴다**([`docs-glossary.md`](../spec/docs-glossary.md) 「디자인 해상도」). 720이면 96×192, 엔진 기본값이면 108×216이다. **§7.3의 1440p 실행을 먼저 돌려 스프라이트의 화면 픽셀을 재고 그 값으로 시트를 만든다.**

- **얼굴은 잘라 낸다.** 이번 캐릭터는 대충 만든 것이라, 얼굴을 보여 주면서 보지 말라고 하는 것은 안 통한다. 이 프로젝트가 그 방식으로 게이트 하나를 이미 버렸다.
- **묻는 방식은 열린 질문이 아니라 양자택일이다.** 「두 인물이 같은 게임의 캐릭터로 보이는가.」
- 체크리스트는 다섯 항목 이하로, 1번이 떨어지면 나머지가 무의미한 순서로 적는다.

- **통과:** 계속한다.
- **실패:** VRoid 베이스를 유지할지 다시 본다. **3D 자체를 접는 판정이 아니다.**

---

## 7. 게이트 2 — 프레임이 게임에서 도는가

### 7.1 무엇을 굽나

**걷기 한 벌, 정면만.** 프레임 6~8장이면 족하다. 4방향은 이번에 안 한다 — 방향 전환은 지금 게임이 이미 하고 있어 미지수가 아니다.

```
3D 마스터 + 걷기 모션 ──▶ 프레임 6~8장(246×493 직접 렌더) ──▶ Cocos
```

**4배로 굽고 줄이지 않는다.** Blender가 목표 해상도로 바로 렌더하고 자체 안티앨리어싱을 쓴다. 줄이는 단계를 넣으면 리샘플러를 새로 짜야 하는데(레포에 없다) 알파 있는 그림은 프리멀티플라이 왕복을 빠뜨리는 순간 윤곽에 어두운 테두리가 생기고, **그건 §6.1의 사람 판정이 「3D 느낌」으로 잘못 읽을 결함이다.** 슈퍼샘플링이 툰 외곽선에 유리하다는 근거가 나오면 Blender 안에서 끝낸다.

**파일명은 자리수를 채운다** — `walk_0001.png` 형태다. `walk_1`·`walk_10`·`walk_2`는 사전순이 어긋나 사람이 클립에 그대로 잘못된 순서로 넣게 되고, 결과인 덜컥거림 역시 「3D 느낌」으로 오분류된다.

### 7.2 Cocos 테스트 씬

**신규 씬 하나. `main.scene`을 열지 않는다.** 씬과 프레임은 `game/assets/test-3d-gate/` 한 폴더에 모은다 — 다음 슬라이스가 판정 결과에 따라 통째로 지우거나 정식 경로로 옮길 수 있어야 한다.

노드 하나에 `cc.Sprite`와 `cc.Animation`을 얹고 프레임을 순서대로 재생한다. 정본이 이미 인정한 수단이다.

> 노드 트랜스폼 애니메이션(숨쉬기 bob·피격 흔들림)과 **잡몹 프레임 재생은 계속 내장으로 한다**
> — [`art-direction.md`](../../design/spec/art-direction.md) §3.2

**Trim을 반드시 끈다.** 이 프로젝트는 임포트 시 자동 트림이 기본이고(`player_4dir_front.png.meta`가 `trimType: "auto"`, 246→215), 씬의 플레이어 노드는 누군가 `_isTrimmedMode: false`로 꺼 둔 상태다. **새로 만든 Sprite는 Trim이 켜진 채로 생긴다.** 켜져 있으면 프레임마다 상자가 달라져 캐릭터가 흔들린다.

레시피에 셋을 번호로 적는다. 임포트한 PNG마다 `Trim Type = None` + Apply · Sprite 컴포넌트의 `Trim` 해제 · `Size Mode = CUSTOM`, `Content Size = 246 × 493`.

### 7.3 게임 크기에서도 본다

**테스트 씬은 246×493로 보여 주는데 게임은 48×96으로 그린다.** 5.1배 차이라, 확대 화면에서만 판정하면 게임이 절대 안 그리는 결함으로 떨어질 수 있다.

노드를 하나 더 두거나 `scale`을 0.195로 두어 **게임 크기 판을 나란히** 놓는다. **720p와 1440p 양쪽에서 실제로 실행해 본다.**

---

## 8. 멈춤 규칙

**이 프로젝트가 네 번 멈춘 방식이 「실패 선언」이 아니라 「조용히 늘어짐」이었다.** 상한이 없으면 다섯 번째도 같은 모양이 된다.

| 게이트 | 상한 | 넘으면 |
|---|---|---|
| 0a 환경 스모크 | 작업 1회차 | 이 장비에서 로컬 Blender가 안 선다고 적고 닫는다 |
| 0b VRM 임포트 | 작업 1회차 | 임포터 문제로 적고 닫는다 |
| **0c 모션 조달** | **작업 2회차** | 리타게팅 애드온으로 한 번 넘어가 보고, 그래도 안 되면 적고 닫는다 |
| 1 화풍 | 작업 1회차 | VRoid 룩이 안 맞는다고 적고 베이스를 다시 본다 |
| 2 프레임·엔진 | 작업 3회차 | 끊긴 지점을 적고 닫는다 |

**넘으면 QA 문서에 실패로 적고 멈춘다.** 「고친다」를 반복하지 않는다.

**그리고 전체 상한 하나.** 위를 다 합쳐 **작업 6회차** 안에 게임 화면에서 움직이는 3D 유래 플레이어를 못 보면, **지금 출하된 4방향 아트를 유지하고 플레이어 스켈레탈 리깅을 v1에서 뺀다.** 이 문장이 이 계획에서 가장 중요한 한 줄이다.

> **게이트별 합(8)이 전체 상한(6)보다 크다.** 게이트별 값은 그 게이트 하나에 쓸 수 있는 천장이고, 전체 상한이 실제로 멈추는 선이다 — 앞에서 여유를 쓰면 뒤 게이트는 천장에 닿기 전에 전체 상한에 먼저 걸린다. 그 관계가 싫으면 전체를 8로 올린다. **회차 수는 전부 제안값이고 승인 시점에 정한다.** **단위는 전부 「작업 회차」로 맞췄다** — 게이트 1이 「렌더 3회」였는데 렌더는 초 단위라 비용의 대리변수가 안 된다. 마음먹으면 하루를 태우고도 3회를 안 넘는다.

---

## 9. 정본과 ADR

**정본 둘을 고친다. 「플레이어 아트가 3D로 간다」는 서술은 안 고친다.** 통과하지 않은 방법을 현재 명세로 세우지 않기 위해서인데, **미루는 것과 「하나도 안 고친다」는 다르다** — 후자로 적으면 정본 게이트에서 거짓 선언을 하게 된다(부록 A-3.8 EH6).

| 정본 | 무엇을 |
|---|---|
| [`ops-licensing.md`](../spec/ops-licensing.md) §2 | 도구 셋에 행을 붙인다 — VRoid Studio · VRM 임포터 애드온 · Quaternius 모션 라이브러리 |
| [`art-asset-spec.md`](../../design/spec/art-asset-spec.md) §9.1.1 | `art-source/`가 PNG뿐 아니라 **3D 소스(`.vrm`)와 판정 증거**도 든다는 것을 적는다 |

6단계에서 `pnpm wf canon-done`에 두 경로를 넘긴다.

**통과하면 그다음 슬라이스가 이렇게 한다.**

- **정본은 「지금 무엇인가」만 든다.** 플레이어 절을 3D 기준으로 다시 쓴다. fal.ai·ComfyUI 서술은 플레이어에서 빠진다.
- **다만 그 서술을 지우지 않고 적용 범위를 좁힌다.** [`art-direction.md`](../../design/spec/art-direction.md) §9의 다섯 중 셋이 AI에 남기 때문이다.

| 카테고리 | 경로 |
|---|---|
| 플레이어 | **3D로 이동** |
| 적 12종 | AI 생성 → 프레임 2~6장. 그대로 |
| 마법 이펙트 | AI 정지 텍스처 + `ParticleSystem2D`. 그대로 |
| 맵·환경 | AI. 그대로 |

- **ADR은 한 장, 최소로 쓴다(2026-09-11 사용자 결정).** 「플레이어 아트는 3D 마스터에서 렌더한다」와 그 이유 한 줄이면 된다. **fal.ai·GPT 웹·ComfyUI의 경위를 다시 서술하지 않는다** — 설계 초기에 깨진 탐색이라 결정 기록으로 승격할 무게가 아니다. 필요한 사람은 ADR 008과 이 문서가 가리킨다.
- **기존 ADR은 고치지 않는다.** 결정이 뒤집혀도 새 ADR을 쓰는 것이 이 프로젝트의 규약이다.

---

## 10. 백로그

신규 **`F105`**를 [`backlog.md`](../backlog.md)에 단다.

### 10.1 `F103`·`F104`를 건너뛰는 것은 의도한 것이다

| ID | 어디서 |
|---|---|
| `F103` | `feat/staff-layer` — 양손 장비 정책(이월 v2) |
| `F104` | `feat/local-art-pipeline` — 그 계획 문서가 선언했다 |

**항목 ID는 영구하고 재번호하지 않는다.** 특히 `F104`는 폐기가 임시라 되살아날 수 있다.

### 10.2 전제가 흔들리는 항목

| 항목 | 무엇이 |
|---|---|
| `F67` 지팡이 분리 | **이 계획이 겨누는 벽이다.** 3D에서 지팡이가 별개 오브젝트라 절차가 통째로 사라진다 |
| `F59` ① Spine | **v2로 밀린다.** v1이 프레임이면 구매도 버전 확인도 v2 일이다 |
| `F64` 발치 오프셋 | 선행 승격이 **취소된다.** `sp.Skeleton`을 v1에 안 쓴다 |
| `F99` 상점 AI 고지 | 플레이어는 AI가 안 들어가지만 **몬스터·배경은 들어간다.** 고지 의무는 그대로다 |
| `F100` 생성 기록 | 플레이어의 재현 단위가 `.vrm`과 `.blend`로 바뀐다. 몬스터는 그대로 |

---

## 11. 경계 · 사람 몫 · 테스트

### 11.1 한다 · 안 한다

**한다.** 환경 설치와 게이트 0a·0b·0c · VRoid 캐릭터 한 명과 `.vrm` 커밋 · CC0 모션 조달과 스크립트 리타게팅 · 툰 셰이딩과 외곽선 · 걷기 프레임 6~8장 정면(246×493 직접 렌더) · 비교 시트 · Cocos 테스트 씬과 `cc.Animation` · 720p·1440p 실행 · QA 기록 · **정본 둘 갱신**(§9).

**안 한다.** 4방향 · 파츠 분리 · Spine · 어깨·축·Pivot · 유료 베이스 · `main.scene`과 기존 플레이어 에셋 · `PlayerController` 교체 · 4등신 정확도 판정 · **Blender에서 비율 리프로포션** · 4배 렌더와 축소 · ADR.

> **4등신은 이번에 판정하지 않지만 렌더 전에 거칠게 밀어 둔다.** 비율을 바꾸면 실루엣과 화풍이 함께 바뀌므로, 안 민 모델로 게이트 1을 통과시키면 생산에 안 쓸 모델을 판정한 것이 된다. **다만 미는 것은 VRoid 슬라이더로만 한다** — Blender에서 아마추어 본 스케일과 스키닝 메시를 함께 미는 것은 이 슬라이스가 감당할 작업이 아니고, §3이 VRoid를 고른 이유가 바로 슬라이더다.

### 11.2 `game/assets/`를 건드린다

| 무엇 | 어디에 |
|---|---|
| 테스트 씬 | `game/assets/test-3d-gate/` 아래 신규 `.scene` 하나. `main.scene`을 안 연다 |
| 프레임 PNG | 같은 폴더. `player_4dir_*.png`를 덮지 않는다 |

**"덮지 않는다"를 선언으로 두지 않는다.** 편집 게이트 훅은 `game/assets/scripts/**/*.ts`만 보므로 아트 PNG 덮어쓰기는 어느 phase에서도 안 막힌다. `.meta`가 남아 있어 Cocos가 조용히 재임포트하면 출하 아트가 바뀐 것을 아무도 모른다. **렌더 스크립트가 출력 디렉터리를 단언하고, 그 자리에 파일이 이미 있으면 덮어쓰기 없이 거부한다.**

**임시 스크립트를 안 만든다.** `cc.Animation` 클립으로 충분하다.

**씬은 지우지 않고 남긴다.** 지우면 QA 문서가 볼 수 없는 씬의 레시피를 들게 된다. `.scene` 삭제는 확인이 필요한 항목이기도 하다.

**`.meta`는 만들지 않는다.** 사용자가 7단계에서 Cocos를 열 때 생기고 8단계에 일괄 커밋한다.

### 11.3 무엇을 사람이 하나

| 일 | 누가 |
|---|---|
| Blender · VRoid · VRM 임포터 설치 | 도구 |
| **VRoid에서 캐릭터 하나** | **사람** — 얼굴을 원하는 대로 밀 수 있는지 함께 확인한다 |
| **VRoid 슬라이더로 4등신 거칠게 밀기** | **사람** |
| **모션 파일 고르기**(CC0 라이브러리에서 걷기 한 벌) | **사람** |
| 리타게팅 · 툰 · 프레임 렌더 · 비교 시트 | 도구 |
| **Cocos 레시피 따라 씬 만들기** | **사람** |
| **화풍 판정**(§6.1) · **움직임 판정**(§7.3) | **사람** |

**레시피는 `docs/qa/blender-3d-gate-test.md`가 든다.** `walking-skeleton-test.md` 수준으로 쓴다 — 씬 생성 메뉴 경로, Canvas와 Camera의 `_layer` 정수값, 임포트 뒤 `Trim Type = None`, 노드마다 어느 메뉴로 만드는지와 Anchor·Size Mode·Content Size, 프레임을 클립에 넣는 순서, 그리고 **어떻게 보는지**(씬을 연 채 상단 재생)까지 적는다.

### 11.4 테스트

`tests/logic/Blender3dGate.test.ts`에 **RED로 먼저** 쓴다.

| 함수 | 무엇을 하나 |
|---|---|
| `frameSetIntegrity(frames, expected)` | **개수가 기대와 같은가** · 프레임들이 **같은 캔버스**인가 · **빈 프레임이 없는가**(알파 ≥ 임계값 픽셀 0개) · **이웃 프레임이 서로 다른가** · **발 밑선이 기준 안**인가 |

**「서로 다른가」가 가장 중요한 단언이다.** 렌더 루프에서 프레임 번호 갱신을 빠뜨리면 여덟 장이 전부 같은 그림으로 나오는데, 파일도 알파도 정상이라 나머지 검사를 전부 통과한다. 그리고 Cocos에서는 「안 움직인다」로 보여 §11.6이 재생 설정을 보라고 엉뚱한 곳을 가리키게 된다. 이웃 프레임의 불투명 픽셀 수가 똑같은 쌍이 하나도 없을 것으로 잡는다.

**빈 프레임 검사는 §4.2의 조용한 실패를 픽셀 쪽에서 거르는 그물이다.** 종료 코드가 0으로 나와도 산출물이 비면 여기서 걸린다.

**알파를 `> 0`으로 판정하지 않는다.** 안티앨리어싱 가장자리에 알파 1~3이 번져 있다. `normalizeAlpha`의 기준값 16에서 시작하고, 실제 분포를 보고 정한 값과 근거를 QA 문서에 적는다.

**순서가 판정을 바꾼다 — 프레임은 `normalizeAlpha`를 지난 뒤에 잰다.** 이 레포의 `footLineY`·`trimBox`는 알파 `> 0`으로 재도록 짜여 있고 임계값 인자가 없다(4방향 정렬에서 그 동작이 맞기 때문이다). 그대로 원본에 대고 재면 발 밑선이 발이 아니라 안티앨리어싱 술의 최하단이 된다. **그 함수들의 시그니처는 안 바꾼다** — 바꾸면 `AiMatting.test.ts`가 회귀한다.

**발 밑선은 상대 편차가 아니라 절대 기준으로 잰다.** 걷기에서는 발 밑선이 정당하게 움직이므로 「프레임끼리 얼마나 다른가」는 판정이 못 된다. 정본이 값을 들고 있다 — 캔버스 246×493에 **발 밑선 y=489**다([`art-asset-spec.md`](../../design/spec/art-asset-spec.md) §12). 모든 프레임의 발 밑선이 `[489 - N, 489]` 안에 들어올 것으로 잡고, **N의 초기값과 첫 렌더 분포를 보고 확정한 값과 근거를 QA 문서에 적는다** — 빈 프레임 임계값과 같은 형태다. 이 기준을 쓰면 프레임 세트가 자기들끼리만 맞고 통째로 다른 높이에 서 있는 경우도 걸린다.

`GATE_OK`·`GATE_FAIL` 줄을 고르는 파서도 여기서 단언한다(§4.2) — 종료 로그가 뒤에 붙은 경우와 그 줄이 아예 없는 경우 둘 다.

**테스트는 합성 픽셀 배열로 돈다.** `AiMatting.test.ts`가 같은 방식이다 — 실제 렌더는 실행기가 판정하지 벤치가 판정하지 않는다. 그래야 `ready-impl`의 RED 게이트가 성립한다.

**실행기는 [`tools/art/judge.ts`](../../../tools/art/judge.ts)의 형태를 따른다.** 판정 로직을 베끼지 않고 부르기만 한다.

**파이썬은 타입체크·lint·vitest 어디에도 안 걸린다.** `tools/**/*.ts`는 셋 다 지나가지만 `.py`는 어느 그물에도 없다. 그래서 Blender 쪽은 굽기만 하고 판정을 전부 TS에 두는 것이고, 이 슬라이스는 그 트레이드오프를 받아들인다.

**`AiMatting.test.ts`가 그대로 통과하는지가 회귀 지점이다.**

### 11.5 통과 조건

**게이트 (기계가 잰다, 이진):**

- [ ] Blender 헤드리스 파이썬이 `--python-exit-code 1`로 돌고 실패 시 0이 아닌 코드를 낸다
- [ ] EEVEE 투명 PNG 출력
- [ ] VRM 임포트 성공
- [ ] 걷기가 골격에서 돌고 프레임이 나온다
- [ ] 프레임 개수가 기대와 같고, 같은 캔버스이고, 빈 프레임이 없고, 이웃끼리 서로 다르다
- [ ] 모든 프레임의 발 밑선이 `[489 - N, 489]` 안
- [ ] `main.scene`과 기존 플레이어 에셋 무변경

**기록 (사람이 본다, 통과·실패 둘 다 적는다):**

- [ ] 96×192에서 3D 느낌이 과도하지 않다 — 비교 시트로(§6.1)
- [ ] 두 인물이 같은 게임의 캐릭터로 보인다
- [ ] Cocos에서 걷기가 재생되고 캐릭터가 흔들리지 않는다
- [ ] 720p·1440p 양쪽에서 확인했다
- [ ] 빌드 파일 개수와 용량을 재서 [`ops-build.md`](../spec/ops-build.md) §6.2의 실측값과 견줬다

**QA 문서의 자동 검증 절은 「통과했다」가 아니라 「판정이 나왔다」를 단언하게 쓴다.** 위 게이트는 실패해도 적고 닫는 것이 정상 경로인데(§8·§11.6), `wf`의 QA 확정 게이트는 그 절에 미체크가 하나도 없을 것을 요구한다. 「통과」로 적으면 **게이트가 실패하는 순간 체크가 남아 사용자 검증으로 못 넘어가고, 실패 근거가 정규 경로로 머지되지 않는다.** 그래서 「`frameSetIntegrity`를 돌렸고 결과는 실패(코드 …)」 형태로 체크하고, 통과·실패 판정 자체는 별도 절에 적는다. `wf skip-test`가 사유를 필수로 받는 것과 같은 형태다.

### 11.6 끊겼을 때

| 어디서 | 무엇을 한다 |
|---|---|
| 0a 환경 | 환경부터. **캐릭터 작업으로 안 넘어간다** |
| 0b VRM | 임포터 버전을 본다. 상한을 넘으면 닫는다 |
| 0c 모션 | 본 이름 대응표를 본다. 상한을 넘으면 리타게팅 애드온으로 한 번 넘어간다(Blender 5.0~5.2) |
| 1 화풍 | VRoid 베이스를 다시 본다. 3D를 접는 판정이 아니다 |
| 2 프레임 | **먼저 프레임이 서로 다른지 본다**(§11.4). 같으면 렌더 루프 문제이지 재생 문제가 아니다. 다르면 Trim · Content Size · 레이어 값 · 재생 설정을 본다 |
| **어디서든** | **게임은 현재 출하된 4방향 아트를 그대로 쓴다. 되돌릴 것이 없다** |

---

## 12. 남기는 것

**폐기 브랜치.** `feat/local-art-pipeline`은 머지하지 않고 `origin`에 보존했다(`39501a5`). 가져올 것은 없다 — 그 브랜치의 함수 셋이 전부 파츠·뺄셈용이라 프레임 경로에서 죽는다.

**ComfyUI 구동 스크립트가 거기 있다**(`pnpm comfy`). **몬스터·배경이 AI 경로에 남으므로 언젠가 다시 꺼낼 수 있다.** 그때 라이선스 함정 하나를 조심한다 — IP-Adapter FaceID·InstantID 계열이 의존하는 InsightFace 사전학습 모델이 **비상업 연구 전용**이고, 커스텀 노드가 자동으로 받아 온다.

**증거 파일의 자리를 정해 둔다.** 비교 시트와 게임 크기 스크린샷을 `.vrm`과 같은 `art-source/player/<날짜>-3d-gate/`에 둔다 — 경로 형태는 정본의 `<카테고리>/<생성일>` 규약을 따르고, 그 폴더가 판정 증거도 든다는 것을 §9의 정본 갱신이 적는다. `docs/temp/`는 추적되지 않아 판정을 돌린 장비에만 남는다.

**인물 폐기 결정의 근거가 지금 `docs/temp/`에만 있다.** 이 계획 §3이 값으로 안고 왔으므로 버전 관리 안에 들어왔다.


> **아래 부록은 2026-09-11 리뷰 시점의 기록이다.** 그 리뷰가 계획을 파츠에서 프레임으로 바꾸게 했으므로, 파츠·어깨·축을 전제한 항목 상당수는 **위 본문에서 이미 사라졌다.** 남겨 두는 이유는 왜 바꿨는지가 여기 있기 때문이다.
# 부록 A — /autoplan 리뷰 기록

> 이 절 아래는 **리뷰 산출물**이고 계획 본문이 아니다. 승인된 항목이 본문에 반영되면 그때 본문이 정본이 된다.

## A-1. Phase 1 — CEO 리뷰 (전략·스코프)

- **모드:** SELECTIVE EXPANSION (autoplan 기본값)
- **시스템 감사:** main `a41f1d7`, 작업 트리 깨끗, 전체 스위트 861건 통과. 코드에 `TODO`·`FIXME`·`HACK` 0건. `TODOS.md`는 없고 백로그 3분할이 그 자리다. 미머지 브랜치 둘(`feat/local-art-pipeline` origin 보존, `feat/staff-layer`)이 각각 `F104`·`F103`을 점유. 최근 30일 변경이 문서·워크플로에 몰려 있고 게임 코드가 거의 안 움직였다 — 아트에 막혀 있다는 진단과 일치한다.

### 0A. 전제 검증

| # | 전제 | 판정 |
|---|---|---|
| P1 | 네 번 다 「그림은 나왔는데 게임까지 못 갔다」에서 멈췄다 | **거짓.** 아래 F1 |
| P2 | 3D가 기하·손·비율을 정의로 준다 | 성립. §5.2가 파츠 경계는 공짜가 아니라고 이미 낮춰 잡았다 |
| P3 | VRoid를 인물 정본으로 삼아도 된다 | 사용자 결정(2026-09-10·09-11). **되돌릴 수 있다** — 기존 인물 파일이 `art-source/`와 게임에 그대로 있다 |
| P4 | 품질 판정을 미뤄도 안전하다 | 성립. §4.2의 화풍 한 장이 가장 큰 위험을 덮는다 |
| P5 | 무료로 다 돈다 | 성립. 다만 **시간 상한이 없다**(F3) |
| P6 | 사용자가 3D를 안 만진다 | 대체로 성립 |

#### F1 — 「게임 화면에 새 플레이어가 뜬 적이 없다」는 사실이 아니다 · critical

§1이 그렇게 적었는데 레포가 반대를 말한다.

> **2-a 4방향 스프라이트: 완료(2026-08-07).** 몸 4장 + 지팡이 1장이 같은 캔버스·같은 발 밑선으로 정렬돼 있고 (…) **2-b 스켈레탈:** 슬롯 분해 규약(§6)대로 파츠를 잘라 리깅한다.
> — [`art-direction.md`](../../design/spec/art-direction.md) §9

확인한 것 셋이다. `game/assets/art/player/player_4dir_*.png` 넷이 전부 **246×493**으로 정본 규격과 일치한다. `main.scene`이 그 UUID를 **다섯 곳**에서 참조한다. 커밋 `b723b5c`의 제목이 「4방향 최종 아트」다.

**즉 멈춘 자리는 「그림 → 게임」이 아니라 「그림 → 파츠 → 리깅」이다.** 2-a는 통과했고 2-b가 한 번도 안 됐다.

**왜 이 정정이 계획을 바꾸나.** 셋이 따라온다.

- **관통이 답할 질문이 좁아진다.** VRoid·Blender·렌더·축소는 2-a가 이미 다른 도구로 해낸 구간이고, 진짜 미검증 구간은 **파츠 분리와 엔진에서의 강체 회전**이다. 사슬 전체를 「미지」로 다루면 이미 아는 것에 시간을 쓴다.
- **폴백이 계획이 적은 것보다 좋다.** §10.6이 실패 시 「기존 경로로 남는다」고 적었는데, 그 경로에는 **이미 출하된 4방향 아트가 있다.** 3D가 전부 실패해도 게임은 지금 상태를 유지한다. 위험을 낮추는 사실이라 밝혀 적는 편이 낫다.
- **인물 폐기의 대가가 과소평가돼 있다.** 버리는 것이 「한 번도 안 통한 인물」이 아니라 **게임에 실려 있고 규격 실측까지 끝난 인물**이다. 사용자가 이미 결정했으므로 뒤집자는 것이 아니라, **되돌릴 수 있다는 사실**을 적어 두자는 것이다.

**조치:** §1의 그 문장을 「2-a는 됐고 2-b가 안 됐다」로 고친다. §10.6에 폴백 상태를 한 줄 적는다.

#### F2 — 훨씬 싼 선행 프로브가 검토된 적이 없다 · high · TASTE

세 검토와 office-hours가 전부 **어떤 3D 소스를 쓸까**만 놓고 갈렸다(무료 베이스 / VRoid / 환경만). **3D를 안 쓰는 선택지는 한 번도 안 나왔다.**

**F1을 받아들이면 질문이 둘로 갈린다.**

| 질문 | 무엇으로 답하나 | 비용 |
|---|---|---|
| ① 파츠 → Cocos → 강체 회전 사슬이 도나 | **지금 게임에 있는 246×493 앞모습**을 넷으로 잘라 얹는다 | 약 1시간, 새 도구 0 |
| ② 3D가 2D 오리기보다 나은 파츠를 주나 | VRoid + Blender | 이 계획 전부 |

**①이 떨어지면 3D는 도움이 안 된다.** 축·층 순서·앵커·Trim·좌표계는 소스가 무엇이든 같기 때문이다. ①을 먼저 돌리면 이 슬라이스에서 가장 위험한 구간(§6 Cocos)을 **VRoid를 설치하기도 전에** 닫는다.

**그리고 ①이 3D의 값어치를 재는 기준선을 만든다.** 2D를 오리면 관절에 가려진 살이 없어 어깨가 비는데, 그게 바로 3D가 고친다고 주장하는 것이다. 두 어깨를 나란히 놓으면 「3D가 낫다」가 주장이 아니라 그림이 된다.

**대가:** 슬라이스에 한 시간이 붙는다. 폐기하기로 한 인물의 그림을 한 번 더 쓴다(버리는 프로브라 무해).

**분류: TASTE.** 사용자가 방금 VRoid로 정했고 범위를 얇게 잡아 달라고 했다. 순서를 바꾸자는 제안이라 최종 게이트에 올린다.

#### F3 — 게이트에 시간 상한이 없다 · high

계획이 「무료」를 반복하는데 **막히면 언제 접는지가 없다.** 이 프로젝트가 네 번 멈춘 방식이 「실패 선언」이 아니라 「조용히 늘어짐」이었으므로, 상한이 없으면 다섯 번째도 같은 모양이 된다.

**조치:** 게이트마다 회차 또는 시간 상한을 적고 넘으면 QA 문서에 실패로 적는다. 게이트 0은 반나절, 게이트 1은 렌더 3회, 게이트 2는 하루를 제안한다.

### 0B. 이미 있는 것 — 무엇을 다시 안 만드나

| 이 슬라이스의 조각 | 이미 있는 것 |
|---|---|
| PNG 디코드·인코드 | `tools/art/PngCodec.ts` |
| 희미한 알파 누르기 | `tools/art/Postprocess.ts`의 `normalizeAlpha`(기준값 16) |
| 알파 기준 상자 | `tests/helpers/SpriteMetrics.ts`의 `trimBox` |
| **게이트 실행기의 형태** | **`tools/art/judge.ts`** — main에 있다 |

#### F4 — 재사용 대상을 `judge.ts`로 이름 지어 둔다 · medium

§10.3이 새 게이트 실행기를 예고하는데 **main에 이미 같은 모양이 있다.** 폐기 브랜치의 `gate.ts`도 「`judge.ts`가 세운 형태를 따른다」고 적혀 있었다. 이름을 안 적어 두면 다음 사람이 세 번째 모양을 만든다.

**조치:** §10.3에 「`tools/art/judge.ts`의 형태를 따른다」를 한 줄 넣는다.

### 0C. 드림 스테이트

```
  현재                          이 계획                      12개월 이상
  4방향 통짜 스프라이트          파츠→엔진 사슬이               Spine 슬롯 교체로
  게임에 실려 있음               도는지 하나 닫는다             v2 스킨을 판다
  파츠 0 · 리깅 0        --->                          --->   잡몹 12종 · 인트로 아트
  잡몹 0장 · 인트로 0장          나머지는 그대로                Steam 출시
```

**델타:** v2 매출로 가는 임계 경로에서 미지수를 **정확히 하나** 없앤다. 잡몹·인트로·게임플레이는 안 움직인다.

### 0D. SELECTIVE EXPANSION — 확장 후보

복잡도 점검: 새 파일 6~8개로 8 이하라 냄새 없음.

| # | 후보 | 판정 | 근거 |
|---|---|---|---|
| E1 | 기존 아트로 1시간 선행 프로브(F2) | **TASTE → 게이트로** | 순서를 바꾸는 제안이라 사용자 몫 |
| E2 | 파츠당 렌더 시간 기록 | **채택** | 나중 18파츠×4방향 규모 산정의 유일한 근거. 비용 0 |
| E3 | VRoid 프로젝트 파일과 VRM을 `art-source/`에 보관 | **채택** | 인물을 다시 못 만드는 사고가 이 프로젝트에 이미 있었다 |
| E4 | 720p와 1440p 양쪽에서 실제 실행 확인 | **채택** | §4.2·§6이 192px 판정을 요구하는데 실행 해상도를 안 적었다 |
| E5 | `tools/blender/`에 README 한 장 | **채택** | 이 레포의 유일한 3D 지식이 될 자리다 |
| E6 | 축 좌표 변환을 순수 함수로 빼서 테스트 | **채택** | 아래 F7 |

### 0E. 시간대별 심문

| 언제 | 무엇이 걸리나 |
|---|---|
| HOUR 1 | Blender·VRM 애드온을 어디서 받고 버전을 어디 적나. 스크립트는 `tools/blender/`인가 |
| HOUR 2-3 | **매핑 표를 무슨 형식으로 어디 두나.** 읽는 쪽이 Python(bpy)인데 커버리지 테스트는 TypeScript다 — 언어가 갈린다(F6) |
| HOUR 4-5 | 어깨 투영의 y 뒤집기. 카메라 종횡비가 246/493=0.4990과 맞는가(984/1972도 0.4990이라 일치) |
| HOUR 6+ | 임시 씬·스크립트 삭제. `.meta` 타이밍. **테스트 씬을 어떻게 실행하나** — Cocos는 열려 있는 씬을 프리뷰하므로 그 씬을 열고 프리뷰한다는 한 줄이 필요 |

### 섹션 1~10

**섹션 1 아키텍처.** 파일을 넘기는 선형 사슬이라 결합이 새로 생기지 않는다.

```
VRoid ──VRM──▶ Blender(bpy) ──PNG 4장──▶ 축소 ──▶ Cocos 테스트 씬
                    ▲                        │
                    └── 매핑 표 ◀────────────┘  ← 유일한 구조적 이음매
                        (모델 고유값을 여기에만 가둔다)
```

되돌리기: 전부 신규 파일이라 브랜치 삭제로 끝난다. **가역성 5/5.**

**섹션 2 오류·구제.** 스크립트 실패 경로가 계획에 없다.

| 어디서 | 무엇이 잘못되나 | 지금 | 있어야 할 것 |
|---|---|---|---|
| VRM 임포트 | 애드온 없음·판 불일치 | — | 게이트 0이 잡는다 |
| 매핑 표 조회 | **뼈·메시 이름 오타** | **없음 ← GAP** | 하드 실패(F5) |
| 축 투영 | 뼈가 카메라 밖 | 없음 ← GAP | 캔버스 밖이면 실패 |
| 파츠 렌더 | 알파가 전부 0 | 없음 ← GAP | 빈 PNG면 실패 |

#### F5 — 조용히 빈 파츠가 나오는 경로 · high

**매핑 표의 이름이 틀리면 그 파츠가 통째로 비어 나온다.** 그런데 파츠를 따로 보면 「그 부위만 없는」 그림이 정상처럼 보인다. `compositeCoverage`가 잡긴 하지만 그건 **전신 렌더가 함께 있을 때만**이고, 계획은 전신 렌더를 산출물로 안 적었다.

**조치 둘.** 스크립트가 매핑 표의 이름을 모델에서 못 찾으면 **즉시 실패**하게 한다(하류 검사에 기대지 않는다). 그리고 **전신 렌더 한 장을 산출물에 명시**한다 — `compositeCoverage`의 `whole` 인자가 그것이다.

#### F6 — 매핑 표가 언어 경계를 넘는다 · medium

Python(bpy)이 쓰고 TypeScript(테스트·게이트 실행기)가 읽는다. 형식과 위치가 안 정해져 있으면 두 벌이 생긴다.

**조치:** JSON 한 장으로 두고 경로를 계획에 못 박는다. 둘 다 읽을 수 있고 손으로 고칠 수 있다.

**섹션 3 보안·위협.** 새 공격면은 **서드파티 Blender 애드온이 Blender 안에서 Python을 실행하는 것** 하나다. 게임 런타임에도 빌드 산출물에도 안 들어간다. 위협 Low / 영향 Low. 완화는 공식 배포처에서만 받고 버전을 기록하는 것이고 §3이 버전 기록을 이미 요구한다. **「공식 배포처에서 받는다」가 안 적혀 있어 한 줄 더한다.** 사용자 입력·인증·비밀·PII가 이 슬라이스에 없다.

**섹션 4 데이터 흐름 엣지.** 축소가 얇은 겹침을 잃는 경로는 §10.3의 두 해상도 검사가 덮는다. 알파 프린지는 임계값이 덮는다. **y 뒤집기 부호 오류**는 팔이 머리 위로 가는 형태라 눈에 바로 띄어 스스로 잡힌다.

**섹션 5 코드 품질.** 순수 함수를 `trimBox` 옆에 두는 배치가 기존 패턴과 맞다. 과설계·미설계 없음. 새 추상화 0개. F4와 F6이 여기 걸린다.

**섹션 6 테스트.** 새로 생기는 것:

```
새 데이터 흐름:   VRM → 메시 → 파츠 PNG → 축소 → Cocos 노드
새 코드경로:      매핑 표 조회 · 축 투영 · 커버리지 판정
새 외부 연동:     VRM 임포터(Blender 안)
새 오류 경로:     F5의 GAP 셋
```

`compositeCoverage` 하나로는 부족하다.

#### F7 — 축 좌표 변환이 순수한데 테스트가 없다 · high

`(x − 123, 246.5 − y)`는 산술이고 픽셀도 파일도 필요 없다. **그리고 이 슬라이스에서 가장 틀리기 쉬운 한 줄이다** — y 뒤집기, 캔버스 절반값, 반올림이 전부 여기 모여 있다. 세 줄짜리 테스트가 잡는다.

**조치:** `pivotToNodeOffset(px, py, canvasW, canvasH)`를 순수 함수로 빼고 `compositeCoverage`와 함께 RED로 넣는다.

**섹션 7 성능.** 파츠 넷 × 1방향 × 984×1972 EEVEE는 초 단위다. 이 슬라이스에 성능 문제 없음. 다만 나중에 18파츠 × 4방향으로 곱해지므로 파츠당 시간을 재 둔다(E2).

**섹션 8 관측성.** 이 슬라이스의 관측 수단은 QA 문서다. §3이 버전을, §10.5가 통과 조건을 든다. **더할 것 셋:** 파츠당 렌더 시간, 파츠별 불투명 픽셀 수, 경계 알파 분포(임계값을 정한 근거).

**섹션 9 배포.** 배포되는 것이 없다. 마이그레이션·플래그·롤백 해당 없음. 유일한 배포성 위험인 `.meta` 타이밍은 §10.2가 덮는다.

**섹션 10 장기 궤적.** 부채는 임시 씬과 임시 스크립트 둘인데 §10.2와 §11이 삭제를 명시했다. **경로 의존이 하나 있다 — 매핑 표 형식이 이후 모든 슬라이스의 인터페이스가 된다**(F6). 지식 집중: bpy 스크립트가 이 레포의 유일한 3D 지식이 되므로 README가 필요하다(E5). 가역성 5/5.

**섹션 11 디자인·UX.** UI 스코프 없음 — 화면 설계·폼·상태가 없다. 건너뜀.

### NOT in scope

E1(선행 프로브)은 게이트 판정 전까지 미정. 그 밖에 이번에 안 하는 것은 본문 §10.1이 든다. 백로그로 미루는 신규 항목 없음 — F1~F7이 전부 이 계획 안에서 닫힌다.

### 실패 모드 정리

| # | 실패 모드 | 보이나 | 조치 |
|---|---|---|---|
| 1 | 매핑 표 이름 오타 → 빈 파츠 | **안 보임 ← 치명** | F5: 하드 실패 + 전신 렌더 산출물화 |
| 2 | 축 y 뒤집기 오류 | 보임(팔이 엉뚱한 데) | F7: 순수 함수 테스트 |
| 3 | 게이트가 조용히 늘어짐 | **안 보임** | F3: 게이트별 상한 |
| 4 | 축소가 겹침을 잃음 | 보임 | 두 해상도 검사 (이미 있음) |
| 5 | 임시 씬·스크립트 잔류 | 보임 | 삭제 체크리스트 (이미 있음) |

### A-1.2 CEO 독립 목소리 (Claude 서브에이전트)

**Codex는 이 장비에 없어 단일 목소리다** — `[subagent-only]`. Codex 자리는 미실행으로 남는다.

독립 리뷰어가 사전 리뷰를 못 본 상태로 레포를 직접 읽고 낸 것이다. **여기 옮긴 항목은 전부 이쪽에서 사실 확인을 마쳤다.**

#### 합의: F1과 같은 결론에 독립적으로 도달 (critical)

같은 정정을 냈고 근거를 더 가져왔다 — `main.scene` 노드 125 `Player`가 `_isTrimmedMode: false`로 4방향 SpriteFrame을 물고, `backlog-archive.md`가 **F62 완료(PR #74, 2026-08-08)**로 적으며, `roadmap.md` §7 아트 체크리스트에 `[x] 플레이어 4방향 스프라이트 — 최종 아트 확보(2026-08-07)`가 있다.

**그리고 실제로 멈춘 자리를 짚었다 — `F67` 파츠 컷·지팡이 분리다.** 2026-08-22에 「지팡이가 머리카락 위를 지나 픽셀로 걷어 내면 그 뒤가 빈다」가 확인돼 원리적으로 막혔다.

**여기서 이 계획에 대한 가장 날카로운 지적이 나온다.** 이 슬라이스가 검증하겠다는 사슬에서 **정말로 미검증인 구간은 파츠 → 리깅**인데, §7과 §10.1이 Spine을 범위 밖으로 빼 놓았다. §6.3이 그것을 이미 인정했으므로, **§10.5의 열다섯 항목이 슬라이스가 실제로 닫는 것보다 많은 것을 주장한다.**

#### C2 — §2.3이 §4.2·§10.4와 정면으로 모순된다 (critical) · **확인됨, 이 계획의 자기모순**

- §2.3: 「게이트용과 생산용 베이스를 다르게 쓰지 않는다」
- §4.2: 「이번 캐릭터는 대충 만든 것이라」 · §10.4: 「캐릭터 하나 만들기 — 30분쯤」

§2.3의 표가 베이스를 바꿀 때 죽는 것을 스스로 열거한다 — 4등신 변형 값 · 파츠 매핑 표 · 어깨 겹침 여유 · 툰 셰이더 · **이미 뽑은 렌더 전부**. 그건 §5와 §6의 산출물 전부다.

**둘 중 하나여야 한다.** ⓐ 주인공을 30분 만에 확정한다(디자인 패스 없이 `art-direction.md` §6의 확정 외형을 대체) — 아니면 ⓑ 베이스가 버리는 것이고, 그러면 **기하 결론이 진짜 캐릭터를 만드는 날 전부 만료된다.** ⓑ면 살아남는 것은 게이트 0뿐이고 그건 슬라이스가 아니라 반나절이다.

#### C3 — 3D에서는 「파츠」와 「프레임」의 구분이 무너진다 (critical) · **미검토 축**

파츠 + 스켈레탈을 고른 이유는 **소스가 2D 한 장**이었기 때문이다. 한 PNG에서 걷기를 못 뽑으니 잘라서 리깅한다. **리깅된 3D는 프레임을 직접 준다** — 이 프로젝트가 잡몹에 이미 쓰는 스프라이트 트랙이다(`art-direction.md` §3.1).

프레임으로 가면 사라지는 것: 파츠 경계(§5.2) · 어깨 겹침(§5.4) · 축 좌표(§5.5) · Pivot 노드(§6.1) · Spine $69와 확인 항목 넷(§7) · Essential 강체 제약 · `sp.Skeleton`의 Size 무효 제약 · `F64`의 선행 승격(§9.2). §10.5가 열다섯에서 넷쯤으로 준다.

**그리고 결정적 사실 하나를 확인했다.**

> ### v1에서 명시적으로 제외
> - 스킨 판매, 트랜스모그
> — [`roadmap.md`](../../planning/roadmap.md) §7

**슬롯 교체가 Spine 의존의 유일한 정당화인데, v1이 그것을 명시적으로 뺐다.**

**이쪽의 반론 셋.** ① 프레임은 애니메이션을 굳힌다 — 타이밍을 고치면 전부 다시 굽는다(다만 스크립트 렌더라 비용이 작아 약한 반론이다). ② 정본이 이미 「v1에 스킨이 없어도 지금부터 슬롯 분해로 만들어야 재작업이 없다」고 결정했다(`art-direction.md` §3.2) — 즉 이건 간과가 아니라 **정본 결정을 뒤집자는 제안**이다. ③ 용량은 걸림돌이 아니다(프레임 판 약 28MB, itch 한도 500MB).

**분류: USER CHALLENGE.** 정본을 뒤집는 제안이라 자동 결정하지 않는다.

#### H1 — `layerize` 실험이 설계돼 있고 한 번도 안 돌았다 (high) · **확인됨**

> `bytedance/seedream/v5/pro/layerize` — 완성 이미지를 **투명 PNG 레이어 2~17장**으로 분해한다. 무엇을 어떻게 나눌지 프롬프트로 지정한다 · 레이어당 $0.03375
> — [`2026-08-07-art-cutout-pipeline-review.md`](2026-08-07-art-cutout-pipeline-review.md) §3

같은 문서 §4가 「부위 분리는 **따로 생성이 아니라 한 장에서 분해**여야 하고 `layerize`가 정확히 그 자리에 있다」고 적었고, §6은 통과하면 **`F59` ①의 파츠 컷·인페인팅과 `F67`까지 흡수한다**고 적었다. §114가 별도 슬라이스로 미뤘고 **그 슬라이스는 안 열렸다.**

**비용이 다섯 레이어 기준 약 $0.17이고 설치가 0이다.** 그리고 **이미 레포에 있는 확정 인물의 그림**에 돌린다.

**이 순서가 뒤집히는 것이 요점이다.** layerize가 통과하면 파츠 문제가 2D에서 풀리고, 그러면 3D로 옮길 이유와 **인물을 폐기할 이유가 함께 사라진다.** 떨어지면 3D 전환이 서사가 아니라 증거를 갖는다.

**분류: USER CHALLENGE.** 슬라이스의 전제를 바꾼다.

#### C4 · H2 — 다섯 번째 파이프라인이고 멈춤 규칙이 없다 (critical) · **확인됨**

| 확인한 값 | |
|---|---|
| `origin/main` 마지막 커밋 | **2026-08-23** (19일 전) |
| 마지막 게임 코드 커밋 | **2026-08-03** (39일 전) |
| 그 사이 작업 | 문서·툴체인 |

그동안 0인 v1 항목: `J3` 메인 메뉴·세이브·도감(우선순위 높음) · `J5` 인트로 씬(높음) · `J6` 오디오 에셋 0개 · `F60` 적 12종 · 마법 이펙트(`ParticleSystem2D` 0건) · 배경 타일맵 · 트레일러·스토어.

**그리고 [ADR 008](../../decisions/008-paid-art-generation.md)이 아직 서 있는데 두 번 우회됐다.** `F70`이 로컬을 철거했고(`a41f1d7`), 그 뒤 `feat/local-art-pipeline`이 ComfyUI를 다시 세웠다(`39501a5`의 `pnpm comfy`). ADR을 뒤집는 ADR 없이 뒤집힌 것이다.

**`CLAUDE.md`의 루프 방지 규칙 — 같은 문제 3번 실패 시 STOP — 이 두 번 통과됐다.**

**조치 제안:** 뒤집는 ADR을 **먼저** 쓰고 거기 멈춤 규칙을 박는다. 「3D가 날짜 X까지 움직이는 플레이어를 게임에 못 넣으면 지금 fal 아트를 유지하고 플레이어 스켈레탈 리깅을 v1에서 뺀다.」

**분류: USER CHALLENGE.** 계속할지 자체를 묻는다.

#### 자동 결정한 수정 — 이 계획의 결함

| # | 무엇 | 심각도 | 조치 |
|---|---|---|---|
| M1 | **축 좌표가 씬 공간에서 틀린다** — 플레이어 노드 contentSize가 **48×96**인데 §5.5는 246×493 기준이다. 5.125배 어긋난다 | high | 테스트 씬 contentSize를 명시하고, 246×493이 아니면 `× (표시 세로 ÷ 493)`를 더한다 |
| H3 | §10.4가 「3D를 만지는 자리 없다」인데 §5.2 폴백 2가 **웨이트 페인팅**이다. §5.2 스스로 폴백 1이 안 될 거라고 적어 폴백 2가 기대 경로다 | high | 사람 열로 옮기고 시간 추정을 단다 |
| H4 | `compositeCoverage`가 **겹침 0px과 30px을 똑같이 통과**시킨다. §10.5의 어깨 항목에 숫자가 없다 | high | `shoulderOverlapPx`를 더한다. 임계값은 기하에서 나온다 — 회전 θ에서 틈 ≈ 반지름 × (1−cos θ), 30°면 약 13% |
| H5 | ±30°로 「구조가 성립」을 말할 수 없다. 정본이 조준 각 회전을 요구한다 | medium | 결론을 「±30°에서 성립」으로 좁히거나 실제 요구 각도로 잰다 |
| M2 | 4등신을 미뤘는데 **그게 검증 대상의 상류**다. 변형하면 파츠 경계와 어깨 기하가 바뀐다 | high | 게이트 1 전에 변형하거나 §10.5의 기하 결론을 전부 잠정으로 표시한다 |
| M5 | 임시 씬의 `.meta`가 8단계에 커밋되고 같은 PR에서 삭제된다. `.scene` 삭제는 확인이 필요한 항목이다 | medium | 삭제 시점을 9단계 중 어디인지 적고 미리 확인받는다 |
| M6 | 인물 폐기 결정(2026-09-10)의 근거가 **`docs/temp/`에만 있고 버전 관리 밖**이다 | medium | 그 결정만이라도 버전 관리 안으로 옮긴다 |
| M4 | 인물 폐기 대가를 §8이 과소 계상 — `art-source/`의 **맨살 베이스 4방향이 재생성 불가**이고 `F61`의 유일한 선행이다 | medium | §8에 더한다. **다만 VRoid가 맨살 베이스를 공짜로 돌려준다** — 계획이 못 한 친-3D 논거다 |
| M3 | 게이트 1이 가장 위험한 질문에 가장 약한 도구다. 요괴 12종은 VRoid로 못 만들어 **3D 주인공 + 2D 요괴**가 화풍 분기다 | medium | 정본 §8-4가 이미 정의한 시험으로 바꾼다 — 툰 렌더 플레이어를 요괴 두 컷 옆 실제 표시 크기에 놓는다 |

### A-1.3 CEO 합의표

```
CEO DUAL VOICES — CONSENSUS TABLE
═══════════════════════════════════════════════════════════════
  차원                            Claude(본)  서브에이전트  합의
  ─────────────────────────────── ──────────  ───────────  ────
  1. 전제가 유효한가              아니오       아니오       CONFIRMED
  2. 옳은 문제를 푸나             부분적       아니오       DISAGREE
  3. 스코프 보정이 맞나           대체로       아니오       DISAGREE
  4. 대안을 충분히 봤나           아니오       아니오       CONFIRMED
  5. 시장·경쟁 위험을 덮나        N/A          N/A          N/A
  6. 6개월 궤적이 건전한가        조건부       아니오       DISAGREE
═══════════════════════════════════════════════════════════════
Codex: 미실행 (이 장비에 바이너리 없음) → [subagent-only]
CONFIRMED 2 / DISAGREE 3 / N/A 1
```

**두 목소리가 전제(1)와 대안 탐색(4)에서 독립적으로 같은 결론에 왔다.** 갈린 셋(2·3·6)은 서브에이전트가 더 강하게 부정한 것이고, 그 근거가 전부 사실 확인을 통과했으므로 **최종 게이트로 올린다.**

**Phase 1 완료.** Codex: 미실행. 서브에이전트: critical 4 · high 5 · medium 6. 합의 2/6 확인, 3건 불일치 → 게이트로. USER CHALLENGE 3건 대기.

---

## A-2. Phase 2.5 — DX 리뷰 (실행 가능성)

- **목소리:** Claude 서브에이전트 단독 `[subagent-only]`. Codex는 이 장비에 없다
- **각색:** 이 계획은 개발자 제품이 아니므로 「DX」를 **이 계획을 실행해야 하는 한 사람이 실제로 실행할 수 있는가**로 읽었다. 대상 둘 — 3D 지식이 없는 사용자, 그리고 두 주 뒤 이걸 다시 여는 사람

### A-2.1 프레임 전환으로 사라진 항목

**이 리뷰가 계획을 파츠에서 프레임으로 바꾸게 한 입력 중 하나다.** 그 전환으로 아래는 대상 자체가 없어졌다. 기록만 남긴다.

| 항목 | 왜 사라졌나 |
|---|---|
| 자식 스프라이트 위치 누락 — 축 노드에 팔을 넣고 스프라이트를 `(0,0)`에 두면 팔이 어깨로 순간이동한다 | 축 노드가 없다 |
| 매핑 표 계약(형식·경로·스키마 버전·좌표 원점) · `layer` 칸이 정본 층 표와 충돌 | 파츠 매핑 표가 없다 |
| `compositeCoverage`의 `whole` 인자에 생산자가 없다 | 그 함수를 안 쓴다 |
| §10.4가 「3D 안 만진다」인데 §5.2 폴백이 웨이트 페인팅이다 | 파츠 분리를 안 한다 |
| 어깨 겹침에 임계값이 없다 · ±30°로 「구조 성립」을 말할 수 없다 | 어깨를 안 잰다 |
| 어깨 이음매를 보이게 하는 마젠타 배경 | 이음매가 없다 |

### A-2.2 본문에 반영된 항목

| 항목 | 어디로 갔나 |
|---|---|
| Trim이 꺼져 있다는 전제가 검증되지 않았다 — 임포트 기본이 자동 트림(246→215)이고 새 Sprite는 Trim이 켜진 채 생긴다 | §7.2 |
| 게이트 0이 첫째도 싸지도 않다 — 설치 셋과 사람 작업 둘을 거쳐야 첫 신호가 나온다 | §5.1 게이트 0a 신설 |
| Cocos 레시피가 슬라이스의 하중을 받는 인계인데 없다 | §11.3 |
| `blender --background`가 스크립트 예외에도 종료 코드 0을 낸다 | §4.2 |
| 재현 불가 — 버전을 기록만 하고 고정·단언하지 않는다. `.vrm`이 커밋 안 된다 | §4 |
| 호출 계약이 없다 | §4.1 |
| 판정에 비교 물건이 없다 — 얼굴을 보여 주면서 보지 말라는 것은 안 통한다 | §6.1 |
| 게임이 그리는 크기의 5.1배에서 판정한다 | §7.3 |
| 멈춤 규칙이 없고 탈출구가 장식이다 | §8 |
| 통과 조건이 기계 판정과 취향을 한 목록에 섞었다 | §11.5 |
| 임시 회전 스크립트가 불필요한 의식이다 | §11.2에서 삭제 |
| 증거 파일의 자리가 없다 | §12 |
| RED 테스트 순서 함정 — 렌더가 없는데 실제 파일로 테스트를 쓰면 `ready-impl`에 못 간다 | §11.4 |
| 본문이 머리말이 주장하는 반영을 안 담았다 | 이번 재작성으로 해소 |

### A-2.3 아직 본문에 없는 항목 — 다음 세션이 받는다

**실패 메시지 규격.** 계획이 실패 출력을 거의 안 정했다. 3D 지식이 없는 사람은 「임포트 실패」로 아무것도 못 한다. 각 메시지는 **문제 · 원인 · 다음에 열 파일**을 담아야 한다.

| 실패 | 메시지가 담아야 할 것 |
|---|---|
| VRM 임포트 실패 | 경로 · 활성 애드온 목록 · 활성화 방법 · **검증된 애드온 릴리스 태그** |
| Blender 버전 범위 밖 | 실제 버전 · 검증된 범위 · EEVEE 식별자가 4.2에서 바뀐 사실 |
| 프레임 알파가 비었음 | 어느 프레임 · 원인 후보 셋(카메라 프러스텀 밖 / `hide_render` / 완전 투명 머티리얼) · 재고 덤프 경로 |
| 프레임 캔버스 불일치 | 어느 프레임이 몇 × 몇인지 · 기준값 · 카메라 해상도 설정 위치 |

---

## A-3. Phase 3 — Eng 리뷰 (아키텍처·테스트·실행)

- **목소리:** Claude 본 + Claude 서브에이전트. Codex는 이 장비에 바이너리가 없어 미실행 `[codex-unavailable]`
- **대상:** 본문 §1~§12. 부록은 리뷰 산출물이라 대상이 아니다 — 서브에이전트는 부록을 열지 않았다
- **사실 확인:** 여기 옮긴 항목은 전부 본 세션에서 파일과 줄 번호로 다시 확인했다. 확인하지 못한 것은 **추정**이라고 달았다
- **테스트 계획 산출물:** `~/.gstack/projects/dev-HongCheol-monster/Choi-HC-feat-blender-3d-gate-test-plan-20260911-071745.md` — 장비 로컬이라 A-3.4가 레포에 남는 판이다

### A-3.1 Step 0 — 이미 있는 것 (무엇을 다시 안 만드나)

계획이 말하는 하위 문제를 레포의 실제 코드에 하나씩 대 봤다. **새로 짜야 하는 순수 함수는 하나뿐이다.**

| 하위 문제 | 이미 있는 것 | 신규? |
|---|---|---|
| PNG 디코드·인코드 | `tools/art/PngCodec.ts` | 없음 |
| 알파 대역 세기 · 트림 상자 | `tests/helpers/SpriteMetrics.ts` `alphaHistogram` · `trimBox` | 없음 |
| **발 밑선 재기** | **`SpriteMetrics.ts:200` `footLineY`** | **없음 — 계획이 안 가리킨다** |
| 알파 잡음 누르기 | `tools/art/Postprocess.ts:44` `normalizeAlpha` (기본 임계값 16) | 없음 |
| 캔버스 정렬 | `Postprocess.ts:108` `alignToCanvas` | 없음 |
| 실행기 형태 | `tools/art/judge.ts` | 본뜬다 |
| 프레임 세트 판정 | — | **신규 1개** |
| 이미지 축소 | — | **없다.** `Postprocess.ts:93`이 "크기를 조절하지 않는다"고 선을 그었다 |

`tools/**/*.ts`는 `tsconfig.tests.json`의 `include`에 들어 있어 타입 검사를 받는다. **파이썬은 안 받는다**(ES2).

### A-3.2 Section 1 — 아키텍처

```
[사람] VRoid Studio ──▶ character.vrm ─────────┐
[ ? ]  걷기 모션 ← 출처가 계획에 없다 (EC1) ───┤  art-source/player/<날짜>-3d-gate/
                                                ▼
              tools/blender/   ← 파이썬. typecheck·lint·vitest 어디에도 안 걸린다
              ├── smoke.py        게이트 0a — 독립. VRM 애드온을 import하지 않는다
              ├── _common.py      VRM 임포트 · 카메라 · 툰 세팅
              ├── import_vrm.py   게이트 0b
              └── render_walk.py  게이트 2 앞단
                                                │  stdout: GATE_OK {json} / GATE_FAIL <code> <msg>
                                                ▼
              game/assets/test-3d-gate/walk_%04d.png   246×493 직접 렌더
                                                │
                      ┌─────────────────────────┴──────────────────────┐
                      ▼                                                ▼
   tools/blender/judge.ts  (실행기)                        [사람] Cocos 테스트 씬
   ├─ decodePng ────────────▶ PngCodec.ts       있음        cc.Sprite + cc.Animation
   ├─ normalizeAlpha ───────▶ Postprocess.ts    있음        Trim 끔 · CUSTOM 246×493
   ├─ frameSetIntegrity ────▶ SpriteMetrics.ts  신규 1개           │
   │   └─ footLineY ────────▶ SpriteMetrics.ts  있음               ▼
   └─ GATE_ 줄 파서 ────────▶ 신규                          720p · 1440p 실행
```

**결합에서 좋은 점.** 굽는 쪽(파이썬)과 재는 쪽(TS)이 PNG 파일 하나로만 만난다. `PngCodec`이 포맷을 알고 `SpriteMetrics`는 배열만 받는 기존 분리와 같은 형태라, 판정이 Blender 버전을 안 타게 된다.

**결합에서 나쁜 점 둘.** ① `tools/blender/`의 파일 구성이 계획에 없다. **게이트 0a는 반드시 독립된 한 파일**이어야 §5.1의 "여기서 막히면 VRoid를 설치하지 않는다"가 성립하는데, 공용 모듈을 거치면 그 전제가 조용히 깨진다. ② `frameSetIntegrity`가 어느 파일에 사는지 계획에 없다. `footLineY` 옆(`SpriteMetrics.ts`)이 맞다 — 거기가 「배열 in, 숫자 out」의 자리다.

### A-3.3 Section 2 — 코드 품질

| # | 무엇 | 심각도 | 목소리 |
|---|---|---|---|
| **EH1** | **`footLineY`는 알파 `> 0`이 하드코딩**(`SpriteMetrics.ts:200-207`)인데 §11.4는 "알파를 `> 0`으로 판정하지 않는다"고 못 박는다. `trimBox`·`footCenterX`도 같다. EEVEE 출력의 안티앨리어싱 술을 발끝으로 읽는다 | high | 서브 |
| **EM7** | §4·§12의 `art-source/player/3d-gate/<날짜>/`가 정본 경로 형태와 다르다 — `art-asset-spec.md:454`는 `art-source/<카테고리>/<생성일>/` | low~medium | 서브 |
| **EM8** | "`player_4dir_*.png`를 덮지 않는다"(§11.2)가 **의도 선언뿐**이다. 훅은 `game/assets/scripts/**/*.ts`만 본다(`gate-scripts.mjs:72`). `.meta`가 남아 있어 Cocos가 조용히 재임포트하면 출하 아트가 바뀐 걸 아무도 모른다 | medium | 서브 |
| **ES2** | `tools/`가 지금은 `.ts`뿐이라 셋 다에 걸린다 — `tsconfig.tests.json`의 `include`, biome, vitest. **파이썬은 셋 다 밖이다.** 받아들일 만한 트레이드오프지만 계획이 그렇게 말해야 한다 | medium | 서브 |
| **ES3** | `.gitattributes`에 `*.vrm`·`*.blend` binary 선언이 없다. NUL 휴리스틱이 GLB를 거의 확실히 잡지만, 그 파일은 `.skel`·`.psd`까지 선점해 둔 방침이다. 빗나가면 `EolPolicy.test.ts:55`의 허용 목록에 안 들어 **GREEN 게이트가 막힌다** | low · 비용 0 | 서브 |

### A-3.4 Section 3 — 테스트 리뷰

**RED 게이트는 성립한다.** 슬러그 변환을 실행해 확인했다 — `toPascal('blender-3d-gate')`가 `Blender3dGate`를 내므로(`.claude/workflow.mjs:460`) `tests/logic/Blender3dGate.test.ts`가 `ready-impl`이 찾는 이름과 같다. 합성 픽스처 방식도 `AiMatting.test.ts`의 헬퍼 그대로 된다.

**테스트 다이어그램 — 새 코드경로와 그 덮개**

| 코드경로 | 신규 | 무엇이 덮나 | 빈틈 |
|---|---|---|---|
| Blender 버전 단언 | 신규 | **없음** | 파이썬. 산출물로만 판정 |
| `GATE_` 한 줄 출력 | 신규 | **없음** | 파서 쪽에 단위 테스트를 건다 |
| `GATE_` 줄 파싱 | 신규 TS | `Blender3dGate.test.ts` | **계획에 없다** |
| 프레임 렌더 루프 | 신규 | **없음** | **전 프레임 동일이 통과한다** |
| 같은 캔버스 | 신규 TS | 피처 테스트 | 거의 항상 참 — 소득이 적다 |
| 빈 프레임 | 신규 TS | 피처 테스트 | 임계값 16은 근거 있다(`Postprocess.ts:45`) |
| 발 밑선 | 신규 TS | 피처 테스트 | **허용차 px가 없다** |
| 축소 | 신규 TS | **없음** | 리샘플러가 레포에 없다 |
| 출력 경로 가드 | 신규 | **없음** | EM8 |
| Cocos 재생 · 두 해상도 | 사람 | QA 수동 절 | 정상 |
| 회귀 | — | `AiMatting` · `EolPolicy` | `footLineY` 시그니처를 안 바꾸면 무회귀 |

**빈틈 넷을 메운다.**

| # | 무엇 | 심각도 | 목소리 |
|---|---|---|---|
| **EH2** | **`frameSetIntegrity`가 「8장이 전부 같은 그림」을 못 잡는다.** 렌더 루프에서 프레임 번호 갱신을 빠뜨리면 파일도 알파도 정상이라 통과한다. 그리고 §11.6이 "재생 설정을 본다"로 안내해 **엉뚱한 곳을 파게 만든다.** §11.4의 "유일한 그물"은 과신이다 | high | 서브 |
| **EH3** | **발 밑선 허용차에 숫자가 없다.** 빈 프레임 임계값 16은 근거와 확정 절차가 둘 다 적혔는데 허용차는 둘 다 없다. 게다가 **걷기에서 발 밑선은 정당하게 움직인다** — 4방향 정지 포즈의 불변식을 시간축에 그대로 옮긴 것이다. 정본에 절대값이 있다(`art-asset-spec.md:529` 발 밑선 y=489)는 것도 안 본다 | high | 서브 |
| **EH4** | **984×1972 축소기가 레포에 없고, 4배로 구울 이유도 안 적혀 있다.** 알파 있는 그림의 축소는 프리멀티플라이 왕복이 필요하고, 빠뜨리면 윤곽에 어두운 테두리가 생긴다 — **그게 §6.1의 사람 판정이 「3D 느낌」으로 잘못 읽을 결함이다** | high | 서브 |
| **EM3** | **`GATE_` 줄을 「마지막 줄」로 읽으면 깨진다**(§4.2). Blender가 스크립트 뒤에 자기 종료 로그를 더 찍기 때문이다(**추정 — 이 장비에서 Blender를 돌려 보지 않았다**). 더 중요한 건 **`GATE_` 줄이 아예 없는 경로**가 규정 밖이라는 것이다 | medium | 서브 |

### A-3.5 Section 4 — 성능

**런타임 성능 위험은 실질적으로 없다.** 프레임 8장·씬 1개다. 대신 다뤄지지 않은 것이 둘 있다.

| # | 무엇 | 심각도 | 목소리 |
|---|---|---|---|
| **EH5** | **`.vrm` 커밋에 크기 판단이 없다.** §4가 "폴리곤·머티리얼·뼈 줄이기를 전부 끈다"고 한 직후에 커밋을 지시한다. 현재 레포 최대 추적 파일이 955KB, `art-source` 전체가 4.2MB, `.git`이 42MB다. **git 히스토리는 영구라 나중에 못 지운다.** 그리고 이건 「로컬 생성 환경 22GB를 철거한다」(`a41f1d7`) 바로 다음 슬라이스다. §10.2는 `.blend`도 재현 단위라 하는데 §11.1의 「한다」에는 없다 | high | 서브+본 |
| **EM1** | **테스트 씬과 프레임이 itch 빌드에 그대로 실린다.** 한도는 안 닿는다 — `ops-build.md` §6.2 실측이 파일 72/1,000, 4.6MB/500MB다. 문제는 **제외 설정이 버전 관리 밖**이라는 것이다. `game/settings/v2/packages/builder.json`은 버전 한 줄뿐이고 빌드 프로파일이 사는 `game/profiles/`는 `.gitignore` 대상이다. Cocos 3.8 빌드 옵션의 Included Scenes로 빼도 그 선택이 이 장비에만 남는다(Context7 공식 문서 확인) | medium | 서브+본 |

`art-asset-spec.md:470`이 **Git LFS를 안 쓰기로 한 근거**를 이미 들고 있고, 재검토 조건을 "레포가 몇백 MB를 넘거나 **같은 큰 파일을 여러 번 갱신**하게 되면"으로 적어 뒀다. `.vrm`을 회차마다 갱신하는 것이 정확히 그 조건이다. 다만 같은 문단이 **LFS가 안 깔린 장비에서 클론하면 `.meta`가 재생성돼 씬 참조가 깨진다**고 적었으므로, 답은 LFS가 아니라 **크기 상한**이다.

### A-3.6 실패 모드 등록부

| # | 실패 모드 | 보이나 | 조치 |
|---|---|---|---|
| 1 | 걷기 모션이 없어 게이트 2가 못 열린다 | **계획 단계에서 안 보임 ← 치명** | EC1 — 모션 조달 게이트를 앞에 낸다 |
| 2 | 8장이 전부 같은 그림 | **안 보임 ← 치명 격차** | EH2 — 상호 상이성 단언 |
| 3 | 프레임 사전순 어긋남 | 보임(덜컥거림) — **다만 사람이 「3D 느낌」으로 오분류한다** | EH2 — 파일명 자리수 고정 |
| 4 | 발 밑선을 안티앨리어싱 술로 잰다 | 안 보임 | EH1 — `normalizeAlpha` 선행 |
| 5 | 축소가 윤곽에 어두운 테두리를 만든다 | 보임 — **역시 「3D 느낌」으로 오분류된다** | EH4 — 직접 렌더로 경로를 지운다 |
| 6 | Blender가 시그널로 죽어 `GATE_` 줄이 안 나온다 | 안 보임 | EM3 — 줄이 없으면 실패로 접는다 |
| 7 | 렌더가 출하 아트를 덮어쓴다 | **안 보임** | EM8 — 기존 파일 있으면 거부 |
| 8 | **게이트가 실패했는데 QA 확정 게이트에 막혀 슬라이스를 못 닫는다** | 막힌 뒤에야 보임 | **EH8 — 아래** |

### A-3.7 EH8 — 계획의 실패 경로가 워크플로 게이트와 부딪힌다 (high · 본 목소리)

**이번 리뷰에서 서브에이전트가 못 본 유일한 고위험이고, 이 계획에만 걸리는 것이다.**

§8과 §11.6은 **게이트가 실패해도 그 사실을 적고 슬라이스를 닫는 것**을 정상 경로로 삼는다. 그런데 `wf`의 QA 확정 게이트가 그것을 막는다. 4종 `pass`가 다 차면 `qaDocClean`이 돌고(`.claude/workflow.mjs:671`), 판정은 **자동 검증 절 안에 미체크가 0이고 통과 근거가 있을 것**을 요구한다(`tests/helpers/QaDoc.ts:88-104`). §11.5의 「게이트 — 기계가 잰다, 이진」 목록을 그 절에 그대로 옮기면, **게이트 하나가 실패하는 순간 체크박스가 남아 `user-verification`으로 못 넘어간다.** 넘어가지 못하면 `approve-pr`도 없고, 실패 근거가 정규 경로로 머지되지 않는다.

수동 체크리스트의 빈 칸은 안 걸린다 — 그 판정은 절 경계를 지킨다(`QaDoc.ts:88-92`). 걸리는 것은 **자동 검증 절에 넣은 이진 게이트**뿐이다.

**조치.** QA 문서의 자동 검증 절은 「게이트를 통과했다」가 아니라 **「판정이 나왔다」**를 단언하게 쓴다. 즉 「`frameSetIntegrity`를 돌렸고 결과는 실패(코드 …)」 형태로 체크하고, 통과·실패 판정 자체는 별도 절로 뺀다. `wf skip-test`가 사유를 필수로 받는 것과 같은 형태다.

### A-3.8 자동 결정한 수정

원칙은 Eng phase 기준 P5(명시적) + P3(실용)이 우선한다.

| # | 무엇 | 조치 | 원칙 |
|---|---|---|---|
| **EH6** | **§9 「정본을 안 고친다」가 §3과 정면 충돌한다.** §3이 `ops-licensing.md` §2 표에 행을 붙이라고 지시하는데 그건 정본이다. 그대로 두면 `canon-skip`이 **거짓 선언**이 되고, 정본 게이트(`workflow.mjs:686`)가 그 거짓말로 통과한다 | §9를 「정본 **둘**을 고친다」로 바꾼다 — `ops-licensing.md` §2(도구 3종 + 모션 출처), `art-asset-spec.md` §9.1.1(`art-source/`가 3D 소스와 판정 증거도 든다). §11.1 「안 한다」에서 "정본 갱신"을 뺀다. 6단계에서 `wf canon-done` 두 경로 | P5 |
| **EH1** | 알파 기준 어긋남 | §11.4에 한 줄 — 「프레임은 `normalizeAlpha`를 지난 뒤에 잰다. `footLineY`·`trimBox`의 기존 기준은 안 바꾼다」. 임계값이 한 곳에만 살고 기존 테스트가 안 흔들린다 | P4·P5 |
| **EH2** | 동일 프레임·순서 | `frameSetIntegrity`에 **상호 상이성**과 **개수 일치**를 더한다. §7.1에 프레임 파일명의 자리수를 못 박는다 | P1 |
| **EH3** | 허용차 없음 | 판정을 **절대 기준**으로 바꾼다 — 모든 프레임의 발 밑선이 y=489에서 N px 안. N 초기값과 "첫 렌더 분포를 보고 QA 문서에 근거와 함께 확정"을 빈 프레임 임계값과 **같은 형태로** 적는다 | P1·P5 |
| **EH4** | 축소기 없음 | 기본을 **「Blender가 246×493으로 직접 렌더한다」**로 바꾸고 §7.1에서 축소 단계를 뺀다. 슈퍼샘플링을 유지할 근거가 생기면 Blender 안에서 끝낸다 | P3·P5 |
| **EH5** | `.vrm` 크기 | §4에 두 줄 — `.vrm` 크기 상한(50MB)을 적고 넘으면 **텍스처 해상도만** 낮춘 판을 커밋한다(구조 보존은 폴리곤·뼈에만 건다). **`.blend`는 커밋하지 않는다** — `.vrm` + Blender 스크립트가 재현 단위다. 실측을 QA 문서에 적는다. **LFS는 도입하지 않는다**(`art-asset-spec.md:470`) | P5 |
| **EH7** | **「4등신 거칠게 밀기」가 도구 칸에 있다**(§11.3). VRoid 기본은 7~8등신이고 이건 아마추어 본 스케일과 스키닝 메시가 함께 따라오는 작업이다. 게다가 §11.1이 이걸 **게이트 1의 선행**으로 못 박았는데 §8 상한 표에 행이 없다 | **VRoid 슬라이더로 한정하고 사람 칸으로 옮긴다.** §3이 VRoid를 고른 이유("슬라이더로 조절한다")와 같은 결이고 조달 리스크가 0이다. Blender 리프로포션은 §11.1 「안 한다」로 | P3 |
| **EH8** | QA 게이트 충돌 | A-3.7의 조치 | P5 |
| **EM1** | 빌드 잔류 | §11.2에 한 줄 — 테스트 씬과 프레임을 한 폴더에 모으고 **다음 슬라이스가 통째로 지우거나 정식 경로로 옮긴다**. §11.5 기록에 "빌드 파일 개수·용량을 재서 `ops-build.md` §6.2 값과 비교해 적는다"를 넣는다 | P3 |
| **EM2** | **96×192가 계산값이고 검증되지 않았다.** 표시 48×96은 실측으로 맞다(`main.scene`의 Player 노드가 `_contentSize` 48×96 · CUSTOM · Trim 꺼짐). 그런데 **디자인 해상도가 프로젝트에 없다** — `project.json`의 `designResolution`에 Fit 정책만 있고 크기가 없어 엔진 기본값을 쓴다(`docs-glossary.md` 「디자인 해상도」 항이 "현재 우리 상태"로 적어 뒀다). 720이면 96×192, 엔진 기본값이면 108×216이다 | §6.1의 비교 시트 크기를 **계산하지 말고 잰다.** §7.3의 1440p 실행을 먼저 돌려 스프라이트의 화면 픽셀을 실측하고 그 값으로 시트를 만든다. §6.1에 「이 값은 §7.3 실측에서 확정한다(초기 추정 96×192)」 한 줄 | P5 |
| **EM3** | `GATE_` 파싱 | §4.2를 셋에서 넷으로 — ④ 실행기는 stdout에서 **마지막으로 `GATE_OK`·`GATE_FAIL`로 시작하는 줄**을 찾고, **없으면 종료 코드와 무관하게 실패로 접는다.** `judge.ts:113`이 같은 사고방식이다 | P1 |
| **EM4** | **실패 코드 목록이 없다.** `GATE_FAIL` 형식만 정하고 코드가 무엇들인지 없다. §11.6은 게이트 단위라 "임포터 버전을 본다"에서 끊긴다 | `tools/blender/README.md`에 **코드 · 무엇이 안 됐나 · 다음에 뭘 하나** 세 칸 표를 만든다. 최소 여섯 — 버전 범위 밖 / EEVEE 식별자 없음 / 애드온 미등록 / `.vrm` 경로·파싱 실패 / 카메라 프레이밍 밖 / 출력 쓰기 실패. 메시지에 **실측값을 박는다**. 본은 `Postprocess.ts:118`과 `typecheck.mjs:58`에 있다 | P1·P5 |
| **EM5** | **§8의 상한 단위가 섞여 있다** — 작업 회차 셋에 **렌더 3회**가 하나 섞였다. 렌더는 초 단위라 비용의 대리변수가 아니다. 마음먹으면 하루를 태우고도 3회를 안 넘는다 | 게이트 1 상한을 **작업 1회차**로 바꾸고 렌더 횟수는 기록 항목으로 내린다. 회차 **숫자 자체**는 사용자 몫이다(B-4) | P5 |
| **ES1** | 애드온은 임의 코드 실행이고 **릴리스 태그는 재작성될 수 있다** | 받은 zip의 SHA-256을 QA 문서에 적고 다른 장비에서 대조한다. `ops-licensing.md` §2 행에 확인 날짜와 버전을 박는다 | P1 |
| **EM7·EM8·ES2·ES3** | A-3.3의 나머지 | 각 행의 조치대로 | P3·P5 |

**`TODOS.md`는 만들지 않는다.** autoplan 기본 절차가 미룬 항목을 `TODOS.md`에 모으라고 하지만, 이 레포는 그 자리가 **백로그 3분할**이고 CLAUDE.md가 그것을 정본으로 세워 뒀다. 위 항목은 전부 이 슬라이스 안에서 닫히므로 미루는 것이 없고, 신규 백로그는 본문 §10의 `F105` 하나뿐이다.

### A-3.9 EC1 — USER CHALLENGE: 걷기 모션이 어디서 오는지 계획에 없다 (critical)

**본문 §1~§12를 다시 훑어 확인했다. 모션의 출처를 말하는 문장이 하나도 없다.** §2.2의 그림이 「3D 마스터 ──▶ 걷기·공격 프레임」으로 건너뛰고, §7.1이 "걷기 한 벌, 정면만"을 굽는다고 하며, §11.3이 「프레임 렌더」를 **도구**(자동) 칸에 넣는다.

**VRoid가 내보내는 `.vrm`에는 모션 데이터가 없다.** 리깅된 휴머노이드 아마추어만 들어 있다. 걷기 사이클은 셋 중 하나로만 생긴다.

| 경로 | 비용 | 걸리는 것 |
|---|---|---|
| ① Blender에서 손 키프레임 | 수 시간 + 기술 | `art-direction.md` §3.1의 "손그림·미감을 최소로"와 정면 충돌 |
| ② 외부 모션 도입 | 조달 + 리타게팅 | **라이선스.** §3의 확인 목록에 모션이 빠져 있다 |
| ③ 절차적 생성 | — | 비현실적 |

**이것이 §8을 무너뜨린다.** 「게이트 2 프레임·엔진 — 작업 3회차」는 **모션이 이미 있다는 전제**의 수치다. 손 키프레임이면 3회차로 안 끝나고, 외부 모션이면 조달·라이선스·리타게팅이 게이트 2 안에 통째로 숨는다. §8이 "이 계획에서 가장 중요한 한 줄"이라 부른 전체 상한 6회차가 근거 없는 값이 된다.

**권고(자동 결정하지 않는다).** 게이트 0b와 게이트 1 사이에 **게이트 0c — 모션 조달**을 넣는다. 여기서 막히면 툰 세팅에 한 시간도 쓰기 전에 닫을 수 있고, 그게 §5.1이 "되돌릴 비용이 가장 작은 지점"이라 부른 논리 그대로다. 그리고 §3의 라이선스 확인 목록과 `ops-licensing.md` §2 표에 **모션 출처 행을 미리 예약**한다.

**분류: USER CHALLENGE.** 어느 경로로 갈지가 슬라이스의 크기와 상한을 바꾸므로 승인 게이트로 올린다.

### A-3.10 Eng 합의표

```
ENG DUAL VOICES — CONSENSUS TABLE
═══════════════════════════════════════════════════════════════
  차원                              Claude(본)  서브에이전트  합의
  ───────────────────────────────── ──────────  ───────────  ────
  1. 아키텍처가 건전한가            부분적       부분적       CONFIRMED
  2. 테스트 커버리지가 충분한가     아니오       아니오       CONFIRMED
  3. 성능 위험이 다뤄졌나           부분적       부분적       CONFIRMED
  4. 보안 위협이 다뤄졌나           부분적       부분적       CONFIRMED
  5. 에러 경로가 처리됐나           아니오       부분적       DISAGREE
  6. 잔류물·머지 위험이 관리되나    아니오       아니오       CONFIRMED
═══════════════════════════════════════════════════════════════
Codex: 미실행 (이 장비에 바이너리 없음) → [codex-unavailable]
CONFIRMED 5 / DISAGREE 1
```

**갈린 5는 본 목소리가 더 강하게 부정한 것이다** — EH8(QA 확정 게이트가 실패 경로를 막는다)을 서브에이전트가 못 봤기 때문이다. 서브에이전트는 §4.2의 종료 코드 진단을 이 계획의 최고점으로 꼽았고 그 판단은 맞다. 다만 그 처방이 계획 밖 게이트에서 한 번 더 막힌다.

### A-3.11 NOT in scope

이번 리뷰가 **범위를 줄이자고 제안한 것은 둘**이고 둘 다 단순화다 — 984→246 축소 단계(EH4)와 Blender 리프로포션(EH7). 나머지는 전부 문장 수정이거나 판정 한 줄 추가다.

**백로그로 미루는 신규 항목 없음.** EC1부터 ES3까지 전부 이 계획 안에서 닫힌다.

### A-3.12 승인 전 최소 수정 (순서대로)

1. **EC1** — 모션 출처 절 신설 · §8 상한 표에 행 · §3 라이선스 목록에 모션 추가 ← **사용자 결정 필요**
2. **EH6** — §9를 「정본 둘을 고친다」로 · §11.1에서 "정본 갱신" 제거
3. **EH8** — QA 자동 검증 절을 「판정이 나왔다」로 쓰도록 §11.5에 명시
4. **EH3 + EH2** — 발 밑선을 절대 기준으로 · 상호 상이성·개수 단언 추가 · 파일명 자리수 고정
5. **EH1** — 「프레임은 `normalizeAlpha`를 지난 뒤에 잰다」 한 줄
6. **EH4 + EH7** — 축소 단계 삭제 · 4등신을 VRoid 슬라이더로 이관
7. **EH5 + EM1 + EM2 + EM3 + EM4 + EM5 + ES1** — 나머지 문장 수정

2~7은 문장 수정 수준이라 비용이 거의 없다. **6은 슬라이스를 실제로 줄이는 쪽이라 §8의 상한을 지키는 데 유리하다.**

> **2026-09-11: 1~7을 모두 본문에 반영했다.** EC1은 부록 C의 조사를 거쳐 **Quaternius CC0 + 스크립트 리타게팅**으로 정해졌고, 게이트 0c가 §5.3으로 신설됐다.

**Phase 3 완료.** Codex: 미실행. 서브에이전트: critical 1 · high 7 · medium 8 · low 2. 본 목소리: high 1 추가(EH8). 합의 5/6, 1건 불일치. USER CHALLENGE 1건(EC1)을 게이트로 올린다.

---

# 부록 C — 걷기 모션 조달 조사 (2026-09-11)

> **이 부록도 임시다.** 경로를 고르고 본문에 반영하면 결정만 §3·§7·§8에 남기고 지운다.

부록 A-3.9(EC1)가 올린 USER CHALLENGE에 답하기 위한 조사다. 묻는 것은 하나다 — **VRoid 인물에 걷기를 입히는 경로가 무엇이고, 각각 무엇을 지켜야 하나.**

확인은 이 장비에서 브라우저로 원문을 열어 했다. **Adobe와 pixiv 두 곳은 봇 차단으로 열리지 않아 표에 그렇게 적었다.** 판정 정본이 요구하는 「원문을 연 날짜」를 못 채운 행은 라이선스 표에 올리기 전에 사용자가 직접 열어 확인해야 한다([`ops-licensing.md`](../spec/ops-licensing.md) §7).

## C-1. 왜 필요한가

VRoid가 내보내는 `.vrm`에는 **리깅된 휴머노이드 골격만 있고 모션이 없다.** 걷기 사이클은 따로 만들거나 가져와야 한다. 계획 본문이 이 단계를 건너뛴 채 「프레임 렌더」를 도구 칸에 넣어 두었다(§11.3).

## C-2. 도구 — 확인된 것

| 무엇 | 값 | 확인 |
|---|---|---|
| **VRM Add-on for Blender** | v4.7.1. 라이선스는 **MIT 또는 GPL-3.0 택일**. Blender **4.2 LTS 지원, 5.3 이상 미지원** | 2026-09-11 · [확장 페이지](https://extensions.blender.org/add-ons/vrm/) · [배포처](https://vrm-addon-for-blender.info/en-us/) |
| **같은 애드온의 VRM Animation** | `.vrma` 임포트·익스포트를 든다(`import_scene.vrma`·`export_scene.vrma`). **익스포트에는 VRM 1.0 아마추어가 필요**하고, 담기는 것은 휴머노이드 본 회전 · Hips 이동 · Expression 프리뷰 값이다 | 2026-09-11 · [문서](https://vrm-addon-for-blender.info/en-us/animation/) |
| **VRoid Studio의 VRM 버전** | v1.20.0부터 **VRM 0.0과 VRM 1.0을 모두 내보내고 내보내기 설정에서 고른다** | 2026-09-11 · [공지](https://vroid.com/en/studio/notice/lqLbniPQLCEfU4ZVxadnN) |
| **Retarget 애드온** | v5.2.0. **GPL-3.0 이상.** 프리셋에 Mixamo와 VRoid가 들어 있다. **Blender 5.0 이상 전용** | 2026-09-11 · [확장 페이지](https://extensions.blender.org/add-ons/retarget/) |
| **애드온 없는 리타게팅** | 본마다 Copy Rotation 제약 + Hips에 Copy Location을 걸고 Visual Keying으로 Bake Action한다. `bpy`로 스크립트가 된다 | 2026-09-11 · 2차 출처 |

### C-2.1 Blender 버전이 두 애드온 사이에 끼인다

**이것이 이번 조사에서 가장 실행에 가까운 발견이다.** §4가 "`bpy.app.version`을 스크립트가 확인하고 범위 밖이면 실패한다"고 정했는데, 그 범위를 두 애드온이 양쪽에서 조인다.

```
VRM Add-on      4.2 ──────────────────── 5.2      (5.3 이상 미지원)
Retarget                        5.0 ──────────────▶ (5.0 이상 전용)
겹치는 구간                     5.0 ── 5.2
```

**Retarget 애드온을 쓰기로 하면 Blender는 5.0~5.2로 묶인다.** 애드온 없이 스크립트로 리타게팅하면 이 구속이 사라지고 VRM 애드온의 범위만 남는다.

### C-2.2 VRoid 내보내기를 VRM 1.0으로 못 박아야 한다

§4는 "폴리곤·머티리얼·뼈 줄이기를 전부 끈다"만 적고 **VRM 버전을 말하지 않는다.** 기본값에 기대면 VRM 0.0으로 나올 수 있고, 그러면 `.vrma` 경로가 막힌다. 내보내기 설정에서 **VRM 1.0**을 고르고 그 사실을 QA 문서에 적는다.

## C-3. 모션을 어디서 얻나 — 경로 셋

| 경로 | 라이선스 | 리타게팅 | 걷기가 있나 | 걸리는 것 |
|---|---|---|---|---|
| **Quaternius Universal Animation Library** | **CC0** — 조건 없음 | 필요(범용 휴머노이드 → VRM 아마추어) | 있다. 두 팩 합쳐 250여 클립 | 리타게팅 한 번. 라이선스 의무가 0이라 정본 표에 행을 붙일 일도 사실상 없다 |
| **Mixamo** (Adobe) | 무료·로열티 없음·상업 이용 허용·표기 의무 없음. **제약은 재배포** — 완성물에 담는 것은 되지만 원본 애니메이션 파일을 에셋 팩으로 되파는 것은 안 된다. 무료 Adobe 계정 필요 | 필요. 또는 Mixamo 자동 리깅 결과를 그대로 렌더 | 있다. 모션캡처 라이브러리 | **원문 확인 실패.** 이 장비에서 Adobe 문서가 403으로 막힌다 — 사용자가 직접 열어 확인해야 표에 올릴 수 있다 |
| **`.vrma` 파일** (BOOTH) | 공식 무료 7종은 저작권이 pixiv에 있고 이용 조건을 따로 본다. 개인 제작자 판은 제작자마다 다르다 | **필요 없다** — VRM 휴머노이드에 그대로 맞는다 | **공식 무료 7종에 걷기가 없다.** 전신 표시·인사·브이·사격·회전·모델 포즈·스쿼트다 | 걷기를 찾으려면 유료이거나 제작자별 조건을 하나씩 본다. pixiv 문서도 봇 차단으로 막혔다 |

## C-4. 이 조사가 권하는 것

**Quaternius CC0 + 스크립트 리타게팅이 가장 싸고 가장 덜 묶인다.**

- **라이선스 의무가 0이다.** CC0라 표기도 재배포 제한도 없다. `ops-licensing.md` §2 표에 행이 붙더라도 「조건 없음」 한 줄이다.
- **애드온이 하나 줄어 Blender 버전이 안 끼인다**(C-2.1). 설치할 것이 VRM 애드온 하나로 유지되고, 그게 §5.1이 "되돌릴 비용이 가장 작은 지점"이라 부른 구조를 지킨다.
- **스크립트가 이 슬라이스의 성질에 맞다.** 본 이름 대응표 + Copy Rotation + Bake는 명시적이고, 실패하면 어느 본에서 틀어졌는지 메시지로 말할 수 있다. 애드온 프리셋은 실패해도 이유를 안 준다.
- **폴백이 있다.** 스크립트 리타게팅이 두 회차 안에 안 서면 Retarget 애드온으로 넘어가고, 그때 Blender를 5.0~5.2로 고정한다.

**그리고 게이트를 하나 낸다.** 게이트 0b(VRM 임포트)와 게이트 1(화풍) 사이에 **게이트 0c — 모션 조달**을 둔다. 묻는 것은 「걷기 사이클이 이 인물의 골격에서 돌아가는가」 하나다. 여기서 막히면 툰 세팅에 시간을 쓰기 전에 닫는다.

## C-5. 확인하지 못한 것

| 무엇 | 왜 |
|---|---|
| Mixamo 라이선스 원문 | Adobe 문서가 이 장비에서 403(봇 차단). 2차 출처로만 확인했다 |
| VRoid 내보내기 문서 원문 | pixiv 도움말이 Cloudflare 봇 검사로 막힌다. VRM 1.0 지원은 VRoid 공식 공지로 확인했다 |
| Blender 5.x에서 VRM 애드온의 실제 동작 | 확장 페이지의 선언값만 봤다. 게이트 0a가 이 장비에서 실측한다 |

---

# 부록 B — 이어서 할 일 (2026-09-14 세션 종료 시점)

> **이 부록은 임시다.** 슬라이스가 끝나면 지운다. 2026-09-11 승인 시점의 판을 이 내용으로 갈았다.

## B-1. 지금 어디인가

| | |
|---|---|
| 브랜치 | `feat/blender-3d-gate` (main `a41f1d7`에서 분기) |
| `wf` phase | **`verification`** (2026-09-14 `start-verification` 통과) — 스크립트 편집이 열려 있다 |
| 커밋 | **없다.** 전부 작업 트리에만 있다. 커밋은 검증 절차의 `pass lint` 뒤에 기능 단위로 한다 |
| 검증 | 타입체크 `full` 통과 · biome 오류 0 · 전체 스위트 899통과/1스킵 · `check-qa`·`check-links`·`check-docs`·`check-meta` 통과 (2026-09-14) |

게이트 다섯이 모두 통과했다. 게이트 2는 판정이 난 뒤 걷기를 다듬느라 4회차까지 써서 상한 3을 넘겼고, 나머지는 1회차 안에서 끝났다.

| 게이트 | 상태 |
|---|---|
| 0a 환경 스모크 | 통과 |
| 0b VRM 임포트 | 통과 |
| 0c 걷기 리타게팅 | 통과 |
| 2 프레임 렌더·Cocos 재생 | 통과. 판정 뒤 걷기 다듬기로 4회차까지 썼고, 후보 H를 게임 폴더에 넣었다 |
| 1 화풍 | 통과(사용자 판정). 첫 의상의 시트로 판정했다 |

## B-2. 이미 정해진 것 — 다시 묻지 않는다

2026-09-11 승인 시점의 결정(3D로 간다 · VRoid · v1은 프레임 · ADR은 통과 뒤 한 장)은 그대로다. 그 뒤에 더 정해진 것이 여덟이다.

| 결정 | 날짜 |
|---|---|
| 멈춤 규칙 회차는 게이트별 1·1·2·1·3, 전체 상한 6 | 2026-09-11 |
| 환경은 Blender 5.2.1 LTS · VRoid 2.3.0 · VRM 애드온 v4.7.1. 값과 해시는 QA 문서 §3 | 2026-09-11 |
| 캐릭터에 옷을 입힌 판으로 판정한다. 속옷 차림은 옷차림 때문에 화풍 판정이 갈려서 버렸다 | 2026-09-11 |
| **팔은 모션을 안 따르고 기준 자세 하나로 고정한다.** 옷이 포즈를 정하지 않게 하려는 것이다 | 2026-09-11 결정, 2026-09-14 값 확정 |
| 게이트 1은 통과다. 이 슬라이스는 그림의 완성도가 아니라 디자인 워크플로가 서는지를 본다 | 2026-09-14 |
| **걷기는 후보 H의 비율로 굽는다**(`SWING_SCALE` — 몸통 0.2 · 허벅지·무릎 0.75 · 발목·발가락 0.35). 남은 어색함은 백로그로 보낸다 | 2026-09-14 |
| 의상을 흰 후드 점퍼로 바꿨다. 게이트 1은 첫 의상의 시트로 판정했다 | 2026-09-14 |
| `walk.anim`의 `Sample`은 12다. 6이 원본 속도지만 끊겨 보인다 | 2026-09-14 |

## B-3. 다음 세션이 할 일

**여기까지 끝났다(2026-09-14).** 게이트 판정 다섯(QA 문서 §10) · 720p·1440p 실행과 디자인 세로 720 실측(§6) · 걷기 후보 H 확정과 게임 프레임 교체(§11) · 비교 시트 · `start-verification` · QA 문서 §8 채움(`check-qa` 통과) · 정본 둘(`ops-licensing.md` §2 · `art-asset-spec.md` §9.1.1) 수정과 `pnpm wf canon-done` 기록 · 백로그 `F105`(3D 전환 후속)와 `F106`(걷기 자연스러움) 추가.

**다음 세션은 검증 절차의 게이트 1부터 한다.** 절차 전문은 `pnpm wf steps`로 다시 본다.

1. **`/cso` → `pnpm wf pass cso`.** 2026-09-14 세션이 시작만 하고 끝내지 못했으므로 처음부터 다시 돌린다. 그 세션이 확인한 것은 이렇다 — 스택은 Node/TypeScript와 Blender용 파이썬이고 CI 워크플로·Docker·IaC가 없다, `.env`와 `.gstack/`은 gitignore 대상이다, 슬라이스 파일에서 비밀 키 패턴이 0건이다. 이전 보고서 셋이 `.gstack/security-reports/`에 있다.
2. **`pnpm typecheck` → `pnpm wf pass ts`.**
3. **`pnpm check --write` → `pnpm wf pass lint`.** 추적 안 되는 `docs/temp/3d-gate/preview.html`도 biome에 걸린다(한 번 걸려 고쳤다).
4. **기능 단위로 커밋한다.** Cocos가 만든 `.meta`는 스테이징하지 않는다(8단계에 일괄 커밋). 커밋 메시지는 heredoc으로 쓴다.
5. **코드 리뷰 → `pnpm wf pass review`.** `superpowers:requesting-code-review`의 템플릿으로 `general-purpose` 서브에이전트를 띄우고, 이슈는 `docs/qa/blender-3d-gate-review-issues.md`에 적는다. 코드를 고치면 `pnpm wf invalidate` 뒤 `/cso`부터 다시 돌고 **`canon-done`도 다시 기록한다** — invalidate가 정본 선언을 함께 지운다. 리뷰에 넘길 후보는 B-4에 있다.
6. 네 게이트가 차면 `user-verification`으로 넘어간다. Draft PR(신규 `.meta` 0개)을 만들고 사용자가 에디터에서 확인한다.

## B-4. 손이 안 간 정리 항목

| 무엇 | 어떻게 |
|---|---|
| `game/.creator/asset-template/scene/test-3d-gate` | **지웠다(2026-09-14 사용자 결정).** 씬을 만들다 잘못 눌러 생긴 Cocos 기본 2D 씬의 사본이었고 쓰는 데가 없었다 |
| `game/assets/test-3d-gate.meta`와 폴더 안 `.meta` | Cocos가 만든 것이라 정상이다. **8단계 `PR 승인` 때 일괄 커밋한다** |
| QA 문서 §6의 `(잠정 96×192)` 둘 | **확정했다.** 1440p 줄은 96×192이고, 더한 720p 줄은 48×96이다 |
| `retarget_render.py`의 덮어쓰기 거부 | 포즈를 조정하던 중 주석 처리돼 있던 것을 **되살렸다(2026-09-14).** 계획 §11.2가 요구한 가드다 |
| `retarget_render.py`의 `retarget_bake` docstring | **고쳤다(2026-09-14).** 「모션 쪽은 T 포즈, VRoid는 A 포즈」라고 적혀 있었는데, 재 보니 두 골격 모두 팔은 T 포즈로 1° 안에서 같고 허벅지만 자기 축으로 180° 돌아 있었다 |
| `retarget_render.py`의 `BASE_ARM_POSE` 주석 | **코드 리뷰에 넘긴다.** 추적되지 않는 `docs/temp/sample.png`를 가리키고, 「Y값을 -75도에서 -58도로 수정하여」처럼 변경 이력을 주석에 적어 두었다 |
| `docs/temp/3d-gate/` | 추적 안 되는 스크래치다. `preview.html`과 `cand_*` 후보 프레임이 걷기 후보 비교에 쓰였고 지워도 된다. 측정값은 QA 문서 §11에 옮겨 두었다 |

## B-5. 자주 쓸 명령

```bash
BLENDER='C:\Program Files\Blender Foundation\Blender 5.2\blender.exe' \
  node --experimental-strip-types tools/blender/gate.ts 0a   # 0a·0b·0c·2
```

`0c`는 `docs/temp/`에 굽고 `2`는 `game/assets/test-3d-gate/`에 굽는다. 렌더 스크립트는 이미 있는 파일을 덮지 않으므로, 출하 프레임을 다시 구우려면 PNG만 지우고 **`.meta`는 남긴다** — `.meta`가 uuid를 들고 있어 함께 지우면 `walk.anim`의 프레임 참조가 전부 끊긴다.

비교 시트는 Blender 없이 `node --experimental-strip-types tools/blender/sheet.ts`로 만든다.

**Cocos가 켜진 채로는 게임 폴더의 PNG를 지우지 않는다.** 에디터가 `.meta`까지 지울 수 있어서, 2026-09-14에는 게이트 0c로 판정한 렌더를 기존 PNG 위에 덮어써 교체했다(QA 문서 §10).

## B-6. 파일 위치

| 무엇 | 어디 |
|---|---|
| 계획 (이 파일) | `docs/development/sessions/2026-09-11-blender-3d-gate-plan.md` |
| QA 문서 — **살아 있는 기록** | `docs/qa/blender-3d-gate-test.md` |
| Blender 도구와 실행 계약 | `tools/blender/` (`README.md`가 실패 코드 표를 든다) |
| 판정 함수 | `tests/helpers/FrameSet.ts` · `tests/helpers/GateLine.ts` |
| 비교 시트 | `tools/blender/ComparisonSheet.ts`(순수 로직) · `tools/blender/sheet.ts`(실행기) |
| 명세 | `tests/logic/Blender3dGate.test.ts` (38개) |
| 마스터와 모션 | `art-source/player/2026-09-11-3d-gate/` (23MB — 새 의상의 `.vrm`이 15.4MB) |
| Cocos 산출물 | `game/assets/test-3d-gate/` (씬·클립·프레임 8장) |
