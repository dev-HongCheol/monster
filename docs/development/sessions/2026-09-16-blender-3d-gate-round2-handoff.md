# 3D 캐릭터 플로우 2라운드 — 세션 인계 (2026-09-16)

- **작성일:** 2026-09-17 (2026-09-16 신설, 최종 상태만 남긴다)
- **브랜치:** `feat/blender-3d-gate` · phase `qa-setup`
- **정본:** [`ops-blender-toon.md`](../spec/ops-blender-toon.md) — 애드온의 실제 MToon 식과 근거 자료를 신설했다. 그 밖에 바뀐 명세는 [ADR 009](../../decisions/009-visual-bounds-exceed-body.md)와 각 게이트 문서에 들어갔다
- **개요:** [2라운드 계획 개요](2026-09-15-blender-3d-gate-round2-plan.md)

---

## 0. 새 세션이 먼저 읽을 것

1. 이 문서 §1 · §3 — 지금 어디이고 다음에 무엇을 하나
2. [G2 문서](2026-09-15-blender-3d-gate-round2-g2-toon.md) §5 · §8 — 남은 판정, 툰 조사 결론, 장비 검토 세트
3. [`ops-blender-toon.md`](../spec/ops-blender-toon.md) — 툰 값이나 외곽선을 만지기 전에 반드시. 애드온이 MToon 명세와 다른 식으로 칠한다

## 1. 지금 어디인가

**G1 통과, G2 막바지.** 가림 판정 통과, MToon 원본 유지, 장비 검토 세트 판정 완료, 외곽선 인버티드 헐 1 확정, 카메라 고도 15°와 발밑 마법진(지름 = 키) 확정(2026-09-17). 남은 것은 **화풍 게이트 판정**(사용자가 720p 크기 그림으로 게임 화면 흉내를 직접 만들어 본다)과 **몬스터 크기 규격 결정**(PC · 모바일을 같은 규격으로 갈지)이다. 둘이 닫히면 G2 통과 조건(G2 §6)을 QA 문서 2라운드 절에 적고 G3로 간다.

| 게이트 | 상태 |
|---|---|
| G0 라이선스 | 통과(2026-09-15) |
| **G1 캐릭터 · 무기** | **통과(2026-09-16)** — 측정 넷, 손 자세 확정, 무기 확정 |
| **G2 툰 · 가림** | **막바지** — 가림 판정 통과, MToon 원본 유지, 장비 검토 세트 판정 완료, 외곽선 헐 1 확정, 카메라 고도 15° · 마법진 확정(2026-09-17). **화풍 게이트 판정과 몬스터 크기 규격만 남음** |
| G3 동작 | 미착수 |
| G4 굽기 | 도구 일부. 15° 고도로 발 · 머리 행 규격을 다시 잡는 일이 생겼다 |
| G5 본편 | 미착수 |

작업은 전부 커밋했다. 미추적으로 남은 것은 Cocos가 만든 `.meta` 11개(PR 승인 때 일괄 커밋)와 `art-source/player/2026-09-16-player-3d/`(생산 `.vrm` — G0 §5.7에 따라 **커밋하지 않는다**)다. `docs/development/backlog.md`에 빈 줄 하나가 더해진 수정이 남아 있는데, AI 작업에서 나온 변경이 아니라 커밋하지 않았다.

## 2. 닫은 것

### 2.1 시각 경계를 몸 경계에서 떼어 냈다 — [ADR 009](../../decisions/009-visual-bounds-exceed-body.md)

지팡이가 캔버스를 넘는 문제를 「무기를 줄인다」로 풀지 않고 **구조로** 풀었다. 246×493은 기준 몸의 소스 캔버스이지 캐릭터가 차지할 수 있는 시각 범위의 상한이 아니다. 무기 · 망토 · 날개 층은 자기 캔버스를 갖는다.

런타임 전 경로를 실제 코드로 확인했고, 시각 크기가 논리 크기로 새는 곳은 **발치 오프셋 하나**뿐이었다. 그것도 Player 자기 노드의 `UITransform`에서 오므로 시각 층을 자식으로 두면 닿지 않는다.

공통 배율은 스칼라 하나다 — `K = 96 / 493 = 0.1947262`. **층 캔버스는 기준과 가로 · 세로 홀짝을 맞춘다**(어긋나면 그 축으로 0.5px 밀린다. 실측으로 확인).

### 2.2 G1 측정 넷

코어 본 52개 일치, 얼굴 · 머리카락 지문 동일, 상의 판들의 맨살이 맨살 판의 부분집합(밖의 점 0개), 맨살 구멍 없음. 무기 자리는 손 위 208px · 아래 284px · 바깥 45px이다.

**VRoid는 옷을 별도 메시로 내보내지 않는다** — `Body` 하나에 머티리얼로만 갈려 있어 층을 가르려면 머티리얼로 면을 골라야 한다.

### 2.3 손 자세 확정

각도를 `pose_bone.rotation_quaternion`에 그대로 넣던 것이 틀렸다. `retarget_bake`는 **월드 공간에서 rest에 곱하는 델타**로 쓴다. `_common.apply_world_delta_pose`가 그 식을 든다. 손가락은 **로컬 회전 + 오일러 XYZ**로 방식이 달라 `apply_local_pose`가 따로 맡는다.

채택값(`retarget_render.py`에 들어감, 근거는 그 주석):

```
J_Bip_R_Hand    (-105, -58,   0)
J_Bip_R_Thumb1  (  25, +20,  30)
J_Bip_L_Thumb1  (  25, -20, -30)
```

### 2.4 무기 확정

`weapons.ts`의 후보 표가 정본이고 `grip`이 무기별 속성으로 들어갔다.

| | 값 |
|---|---|
| 지팡이 전장 | 0.883m (키의 4/5) |
| 지팡이 대 반지름 | 0.012 (게임 720p에서 2.1px) — 사용자 확정(2026-09-16) |
| 지팡이 그립 | `[-0.005, 0.015, 0.57]` — 아래 끝이 바닥에서 30px |
| 방패 바깥 반지름 | 0.141 (종전의 절반) |
| 방패 그립 | `[0, 0.07, 0.75]` — 판 중심, y는 손이 판 뒤로 들어가는 몫 |

### 2.5 G2 가림 판정 통과

| | 지워짐 | 새로 생김 |
|---|---|---|
| 지팡이 | 49,228 | **0** |
| 방패 | 45,843 | **0** |

「새로 생김 0」이 판정이다. Holdout이 몸에 덮인 자리만 지웠다. 수치는 반지름 0.012로 구운 탐침 이미지(`docs/temp/3d-gate/g2/N_*` · `L_*`)를 `layers.ts subset`으로 잰 값이다.

### 2.6 툰 조사, 장비 검토 세트, 도구 정리

- **MToon 값은 VRoid 원본을 유지한다(사용자 판정).** 조명 방향 · 문턱 · 계단 정도를 바꾼 후보를 봤는데 원본이 가장 좋았고, 음영 차이가 가장 큰 얼굴은 흉터처럼 보였다. 상세는 G2 §8.1.
- **애드온의 실제 음영 식이 명세와 다르다.** 0보다 큰 음영 경계는 효과가 없고, 빛을 등진 면은 값으로 못 밝히고, 태양 고도에 따라 그늘 폭이 달라진다. 근거 자료와 식은 정본 [`ops-blender-toon.md`](../spec/ops-blender-toon.md).
- **장비 검토 세트를 구웠다.** 망토 · 날개(사용자에게 같은 슬롯이지만 깨지는 방식이 달라 둘 다) · 금속 장비 · 얇은 장식 · 오라. 수치와 방법은 G2 §8.2.
- **망토는 판정을 두 번 받아 세 번째 판까지 구웠다.** 첫 판은 윗단이 뒤통수에서 시작했고 폭이 소매 폭이었으며 평평한 한 색이라 판정할 것이 없었다 — 목 밑 높이 · 어깨 폭 · 윗단부터 잡힌 주름 · 안감 판으로 고쳤고, 조명이 앞 위라 세로 주름이 옷 문턱으로는 그늘지지 않아 망토만 문턱 0.8을 줬다. 두 번째 판은 위치 · 폭 · 주름 · 안감이 통과했고, 흔들림이 좌우로 보이던 것(주름 위상 돌리기 → 아래로 내려가는 물결 + 호 길이 보정)과 허리에 상의가 비치던 것(등 뒤 10 → 13cm, 상의 옷단 0.094)을 고쳤다. 수치와 계산은 G2 §8.2.
- **날개는 첫 판정을 받아 다시 구웠다.** 뿌리가 등을 고려하지 않고 가슴 높이 4.5cm 뒤에 떠 있었고, 퍼덕임이 깊이 축 회전이라 뿌리 아랫부분이 등에서 떨어졌다 붙었다 했다. 날개뼈 열의 등 표면을 재서 뿌리 면 중심을 날개뼈(x ±0.045, y 0.068, z 0.65)에 붙이고, 경첩을 뿌리 세로 모서리로 바꿔 나비처럼 0~70° 접히게 했다. 두 번째 판은 통과했다(2026-09-17). 수치는 G2 §8.2.
- **얇은 장식은 판정을 받았다.** 720p에서 술(0.8cm)은 사라지고 사슬(2.2cm 고리)은 점으로 남는다. 위치 지적(뒷모습 허리 옆으로 삐짐, 3/4에서 앞에 떠 있음)은 허리 앞면을 재서 좌표만 코드에서 고쳤고 다시 굽지 않았다 — 외곽선 후보 굽기에서 본다.
- **화려한 장비는 사진 기준으로 다시 만들었다(2026-09-17).** 첫 판의 어깨 구는 관절보다 7.7cm 위에 떠 있고 재질에 반사점이 없어 「흰 계란 두 개」로 보였다. 사용자 사진(어깨갑옷 + X자 끈 + 뿔)대로 금색 금속으로 옮겼고, 끈은 등에서도 교차, 배판, 명치 보석을 더했다. 그러려고 몸 표면 투영(`weapons.py Surface` — `wrap` · `cap` · `strap` · `horn` · `snap`)과 금속 matcap(`toon.py matcap_image`)을 도구에 더했다. G2 §8.2.
- **오라는 발밑 마법진으로 바뀌었다(사용자 결정 2026-09-17).** 몸을 감싸는 껍데기 대신 워크3 패시브처럼 바닥에서 회전하는 마법진이고, 프레임에 굽지 않고 게임에서 별도 노드로 얹는다(정원 그림 + 부모 세로 배율 = 카메라 고도 사인 + 자식 회전). 검토 세트에서 뺐고, 카메라 고도 후보 시트에 원판을 넣어 고도를 고른다. G2 §8.2.
- **탐침 결함 하나를 고쳤다.** 3/4 방향에서 모델을 돌린 뒤 몸 상자를 재서 배율 검사에 걸렸다. 돌리기 전에 재도록 바꿨다(G4 §3 카메라 공유와 같은 방향).
- **`docs/temp/`의 스크립트를 전부 정리했다.** 다시 쓸 것은 `tools/blender/`로 옮기고(§4) 일회성은 지웠다.

## 3. 다음 한 칸

### 3.1 화풍 게이트와 몬스터 크기 — 사용자 판정 대기

크기 판정은 `mock.ts`가 만든 게임 화면 흉내로 한다 — 이 장비의 `docs/temp/3d-gate/elevation/mock_a_720p.png` · `mock_b_720p.png`(1280×720, 상의 A · B). 플레이어는 규격의 80%, 몬스터 넷(달걀귀신 · 처녀귀신 · 도깨비 · 두억시니)은 `collisionRadius`를 직선으로 옮긴 크기(50 · 58 · 73 · 75)다. 첫 흉내(`mock_720p.png`, 모바일 `mock_iphone16pro.png`)는 플레이어 96 · 도깨비 70 · 처녀귀신 50으로 일회성 스크립트가 만든 것이고 그 판정이 아래 2번을 낳았다. 사용자가 Gemini로 직접 꾸밀 때 쓰는 720p 낱장은 같은 폴더의 `player_p15_720p.png`(상의 A + 마법진, 117×107) · `player_top_b_p15_720p.png`(상의 B) · `ghost_1_720p.png`(도깨비) · `ghost_2_720p.png`(처녀귀신)이고, 낱장은 아직 규격 크기(96 · 70 · 50)다. 귀신 표본 원본은 `docs/temp/3d-gate/g2/`의 `달걀귀신.png`(1136×1385) · `처녀귀신.png`(636×904) · `도깨비.png`(1145×1374) · `두억시니.png`(1141×1379, 투명 배경으로 다시 받음)이고 커밋하지 않는다.

새 세션이 받을 판정은 둘이다.

1. **화풍 게이트**(G2 §5 사람 판정 둘) — 3D 플레이어가 2D 플레이어 · 귀신과 한 게임으로 읽히나, VRoid 기본 룩으로 읽히지 않나. 결과를 G2 §5 체크박스와 §8.4에 적는다. 통과하면 G2 §6의 넷이 다 닫힌다.
2. **몬스터와 플레이어의 크기** — 첫 흉내를 본 사용자가 플레이어는 너무 크고 몬스터는 너무 작다고 봤다(2026-09-17). 정한 것 셋: 플레이어는 규격의 80%(96 → 77 안팎), 몬스터 크기의 기준은 `threatScale`이 아니라 `collisionRadius`(판정이 몸에 비례한다 — 그래야 판정이 가장 큰 두억시니가 도깨비보다 크다), 양 끝만 고정해(달걀귀신 50 · 두억시니 75) 사이를 직선으로 배분한다. 이 크기의 흉내를 보고 판정하며, 통과하면 규격([`art-asset-spec.md`](../../design/spec/art-asset-spec.md) §4 · 플레이어 표시 크기)과 판정값(`player.json`의 피격 사각형 · 이동 원, `enemies.json`)을 그 뒤에 재조정한다. 플레이어 80%가 판정 · 세팅에 미치는 사전 점검 결과는 G2 §8.5.

### 3.2 그다음 — G2 닫기와 G4 규격

- G2 §6 통과 조건 넷 중 셋(가림 알파 0 · 층 합성 테두리와 수치 · 외곽선과 고도)이 닫혔고 화풍 게이트만 남았다. 닫히면 판정 결과를 `docs/qa/blender-3d-gate-test.md` 2라운드 절에 적는다.
- **출하 프레임의 발 · 머리 행 규격을 15°로 다시 잡는다**(G4). 고도가 있으면 발밑 점이 489행에서 위로 올라간다(15°에서 485.1행 — `probe_layers.py`가 판정 줄에 `ground_px`로 준다). `PLAYER_FRAME_SPEC`과 `FootprintLogic`의 발치 오프셋이 같이 걸린다.
- **정본 갱신(G6)** — 외곽선(인버티드 헐 1 · 폭 0.0062m · 눈 · 얼굴 그림 제외 · 몸 헐은 가림 포함), 고도 15°, 마법진(위에서 본 정원 텍스처를 게임 노드로, 세로 배율 sin 15° = 0.26, 지름 = 키, 캐릭터 뒤), 천 그늘 문턱 0.8 후보, 금속 matcap, 몸 표면 투영을 어느 정본에 적을지 정한다. `ops-blender-toon.md` §5는 이미 고쳤다.

### 3.3 겨냥할 수치

사용자가 만든 귀신 표본 둘을 재서 목표를 뽑았다(`docs/temp/3d-gate/g2/처녀귀신.png` · `도깨비.png` — 커밋하지 않는다).

| | 처녀귀신 | 도깨비 |
|---|---|---|
| 윤곽선 굵기 | 5px = 긴 변의 **0.55%** | 8px = **0.57%** |
| 평균 채도 | 0.200 | 0.427 |
| 뭉갠 색 수 | 337 | 1092 |

**윤곽선 비율이 둘 다 0.55~0.57%로 맞아떨어진다.** 493px 캔버스 기준 **2.7~2.8px**이 외곽선 목표다. 잰 방법은 어두운 픽셀(명도 0.22 미만)의 가로 연속 길이 중앙값이었고, 그 스크립트는 정리하며 지웠다.

**채도는 목표로 쓰지 않는다.** 두 표본이 두 배 넘게 벌어지는데 소재 차이다 — 정본 §4.1의 `tint`가 처녀귀신을 `#C9C9D2`, 도깨비를 `#5A3A8A`로 정해 놨다.

## 4. 도구 쓰는 법

전부 `tools/blender/`에 있고 Blender는 `C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`다. TS 실행기는 환경 변수 `BLENDER`로 이 경로를 받는다(Git Bash 형식 경로는 Node가 못 푼다).

```bash
# 층 하나 굽기 — layer: body · top · staff · shield · gear · whole
blender --background --python-exit-code 1 --python tools/blender/probe_layers.py -- \
  --base-vrm art-source/player/2026-09-16-player-3d/player_base.vrm \
  --layer staff --weapon-spec docs/temp/3d-gate/g2/staff_orb.json \
  --out docs/temp/3d-gate/g2/L_staff.png \
  --width 246 --height 493 --foot-row 489 --head-row 2 \
  --layer-width 420 --layer-height 493
```

- `--no-holdout 1` — 가림을 끈다. **층이 비어 나올 때 가려져서인지 애초에 없어서인지를 가르는 유일한 수단**이다
- `--yaw 180` — 모델을 돌린다(뒷모습). 몸 상자는 돌리기 전에 잰다
- `--pitch 30` — 카메라 고도(도). 몸 중심을 겨냥한 채 내려다보고 직교 배율은 그대로다. 발 · 머리 행은 0°에서만 정확하다
- `--top-vrm <경로>` — 상의 층에 필요하다
- `--gear-spec <json>` — 장비(망토 · 날개 …)를 사양의 `bone`에 붙인다. `gear` 층은 몸으로 가리고, `whole`은 가림 없이 함께 굽는다. 몸에 붙는 부품(`wrap` · `cap` · `strap`, `snap`)은 `weapons.py Surface`가 몸 메시로 광선을 쏴 표면에 맞춘다 — 머리카락은 뺀다
- `--staff-spec` · `--shield-spec` — `whole`에 무기를 든다(없으면 빼고 굽는다)
- `--toon <json>` — 툰 사양. 키와 의미는 `toon.py` 머리 주석. `shade_threshold`(0~1)가 명세의 `shading_shift`를 대신한다. 무기(`WEAPON`) · 장비(`GEAR`)는 사양에 그 분류를 적어야 MToon으로 바뀌고, `like`로 VRoid 부위의 음영 규칙을 복사한다. `matcap_image`(절대 경로)로 금속 반사점을 준다 — `gear.ts`가 만들어 넘긴다
- 무기 사양 JSON은 `node --experimental-strip-types tools/blender/weapons.ts --dump-chosen docs/temp/3d-gate/g2`가 뽑는다

| 도구 | 무엇 |
|---|---|
| `probe_layers.py` | 층 하나를 가림 전용 몸과 함께 굽거나, 기준 컷(`whole`)을 굽는다 |
| `toon.py` | 툰 사양을 MToon 머티리얼에 입힌다 |
| `inspect_mtoon.py` | MToon 값 덤프 + 애드온이 어느 음영 식을 쓰는지 보고(`shading_path`). 애드온 · Blender 판을 바꾸면 먼저 돌린다 |
| `gear.ts` | 장비 검토 세트 굽기 · 시트 · 수치 · 흔들림 재생. `--only cape,wings`, `--sheet-only`(굽지 않고 시트만). 경우별 `toon`(툰 값 덮기) · `views`(방향 — 왼쪽 측면 yaw 300은 화려한 장비만) |
| `outline.ts` · `outline.py` | 외곽선 후보를 표(`METHODS`)로 두고 시험 세트로 굽고 시트 · 수치를 만든다. `--only hull2,post2` · `--cases cape,wings` · `--views front` · `--sheet-only`. 후처리 선은 TS가 긋고, Line Art와 패스는 `probe_layers.py`의 `--lineart` · `--passes`가 받는다 |
| `elevation.ts` | 카메라 고도 후보를 발밑 마법진과 함께 굽고, 출하 2D · 귀신 표본과 실제 표시 크기로 나란히 놓는 비교 시트. `--pitches 0,15,30,45`. 고른 고도(15°)는 상의 B도 함께 구워 720p 그림을 두 판 다 뽑고, 3D 칸의 720p 그린 높이를 허용 범위와 견줘 배율 사고를 막는다. 다섯을 동시에 굽는다(`gear.ts`의 `bakeAsync` · `runPool`) |
| `mock.ts` | 720p 게임 화면 흉내. 고른 고도의 플레이어(상의 A · B, 규격의 80%)와 귀신 표본 넷을 실제 표시 크기로 1280×720에 흩어 놓는다. 몬스터 크기는 `enemies.json`의 `collisionRadius`를 직선으로 옮긴 값이고 놓은 그림마다 보이는 높이를 기대값과 견준다. `elevation.ts`를 먼저 돌려야 한다 |
| `layers.ts` | `subset`(가림 판정) · `stack`(층 합성 + 720p) · `diff`(기준 컷과의 차이) |
| `bake_layer.py` | 층 캔버스를 달리해 굽는다(ADR 009 실증에 씀) |
| `inspect_meshes.py` | 메시 · 머티리얼 · 정점 지문 덤프 |
| `measure_weapon_room.py` | 기준 자세의 손 위치와 몸 상자 |

## 5. 일하는 방식 — 사용자에게 받은 교정

**이미지가 산출물이면 AI가 판정하지 않는다. 경로를 주고 사용자 확인을 받은 뒤에 다음 단계로 간다.** 2026-09-16에 자세를 고친 렌더를 AI가 스스로 통과시켰는데, 사용자가 보니 오른손은 손바닥이 정면을 향했고 왼손 엄지는 뒤집힌 따봉이었다. 고친 뒤의 확인도 같다 — 세 번째 망토에서 상의가 안 비치는지를 AI가 탐침을 따로 구워 픽셀로 세자, 이미지는 사용자가 더 빨리 정확히 본다고 다시 교정받았다. 코드 측정은 굽기 전 수치 설계(본 위치 · 표면 거리)까지이고, 나온 이미지의 확인은 경로와 볼 곳을 주고 맡긴다.

**조명이 만드는 것은 굽기 전에 식으로 계산한다.** 첫 망토는 앞 위 조명에서 세로 주름이 그늘질 수 없는데도 구워서 판정에 올렸고, 사용자는 「평평한 분홍」을 봤다. `ops-blender-toon.md` §3.2의 식에 법선을 넣으면 굽기 전에 나오는 답이었다.

**움직임은 카메라 축을 뺀 성분으로 설계한다.** 두 번째 망토의 흔들림은 끝자락 젖힘(깊이)과 주름 위상 돌리기(좌우)였는데, 뒷모습에서 깊이는 안 보이고 좌우만 남아 「위아래가 아니라 좌우로 흔들린다」는 판정을 받았다.

**시트도 수치로 검증한다.** 2026-09-17 고도 후보 시트에서 3D 칸이 세로 1.46배 늘어난 채 판정에 올라갔다(굽기 캔버스와 시트 배율의 불일치). 칸을 만들 때 트림 상자 높이가 기대값(720p에서 96px)인지 재면 잡혔다. 시트 실행기가 이제 그 검사를 한다(`elevation.ts`의 `checkCellHeight`, 2026-09-17 — 허용 범위와 첫 판이 걸린 이유는 G2 §8.4).

**재는 것은 코드로, 보는 것은 사람이.** 이 라운드의 탐침 버그 여덟 개 중 일곱 개를 숫자가 먼저 잡았다(`ortho_scale` 불일치 · 무기 world bbox · yaw 후 손 위치 · 알파 부분집합 · 3/4 배율 …). 눈으로 본 것 하나는 틀렸다.

**도구가 예상과 다르게 동작하면 시행착오를 늘리지 말고 공식 자료 · 소스부터 본다.** 음영 경계 값이 안 먹자 진단 렌더를 거듭하다 사용자가 멈추고 자료부터 확인하라고 했다. 노드를 읽고 나서야 원인(명세와 다른 식)이 한 번에 나왔다. 확인한 자료는 정본으로 남긴다.

**개념 · 영향도 질문에는 계산하지 말고 바로 답하고, 결론을 첫 줄에 둔다.** 「망토가 붙으면 영향이 뭐냐」에 부위 지도 렌더를 돌리다 중단당했고, 망토 위험을 목록 속에 흩어 쓰자 「망토가 문제된다는 거 아니야?」라는 반응이 나왔다.

**예외 경우가 보이면 말로 경고하지 말고 만들어서 검토하고 간다.** 장비 검토 세트가 그렇게 생겼다.

**`docs/temp/`에 스크립트를 두지 않는다.** 다시 쓸 도구는 `tools/`에 커밋하고, 일회성 진단은 레포 밖 스크래치에 둔다. `docs/temp/`에는 사람이 볼 시트 · 이미지 · HTML만 둔다.

**위치 수정은 이 형식으로 주고받는다** — ① 어느 이미지 ② **화면 기준** 방향(「캐릭터 왼쪽」은 앞뒤에서 뒤집힌다) ③ 픽셀 또는 물체 기준 비율.

**굵기 · 크기 판정은 게임 크기 축소판으로 한다.** 탐침 렌더는 실제보다 여러 배 크게 보인다.

## 6. 열어 둔 것

- **무기(지팡이 · 방패)의 재질** — 원본 컷에서 무기는 Principled BSDF(스페큘러 · 부드러운 명암)로 칠해진다. 몸에 맞춰 MToon으로 바꿀지 사용자가 아직 정하지 않았다. 툰 사양에 `WEAPON`을 적으면 바뀐다
- **발밑 마법진** — 게임에서 얹는 별도 노드다(G5 본편 항목). 위에서 본 정원 텍스처를 자식으로 두고 부모의 세로 배율을 sin 15° = 0.26으로 눌러 자식을 회전시킨다. 지름은 캐릭터 키(96단위), 중심은 발밑 점, 캐릭터 뒤(G2 §8.4). 텍스처 그림은 아직 없다 — `elevation.ts`의 절차 그림(고리 둘 · 방사선 · 룬 자리)은 크기 확인용이다
- **천의 그늘 문턱** — 몸 조명이 앞 위라 세로 주름은 옷 문턱으로 그늘지지 않는다(G2 §8.2). 망토는 문턱 0.8을 후보로 받았고, 천 재질을 확정할 때 그 값을 툰 사양과 정본에 적어야 한다. 그늘 텍스처 없이 기하로만 주름을 보이려면 이 길뿐이다
- **가는 장식의 최소 굵기** — 720p에서 지름 0.8cm(0.7px)는 사라지고 2.2cm 고리는 점으로 남는다(G2 §8.2). 장비 디자인 규격에 적을 후보다 — G6 정본 개정 때 `art-asset-spec.md`에 넣을지 정한다
- **무기 재질과 헐** — 외곽선이 인버티드 헐로 정해졌고 헐은 MToon에서만 나오므로, 무기도 MToon(상의 규칙 · `shade_ratio` 0.6)으로 바꾸는 쪽이 사실상 정해졌다. 사용자에게 확인받아 정본에 적는다
- **`F108`** — 귀신 생성 요구사항을 플레이북 정본으로 승격. 플레이북이 fal.ai · 플레이어 2D 기준이라 지금은 못 받는다
- **크기 판정 뒤 재조정** — §3.1의 2번. 크기가 통과하면 `art-asset-spec.md` §4(적 치수표 · 플레이어 표시 크기 · §4.1 공식)와 `player.json`(피격 사각형 · 이동 원) · `enemies.json`(`threatScale` 또는 크기 유도 규칙)을 고치고, 적 스프라이트 노드 크기(코드)와 3D 배율 `K`(ADR 009)도 따라간다. 무엇이 걸리는지는 G2 §8.5
- **정본 갱신 기록** — `wf canon`이 `ops-blender-toon.md`를 이 슬라이스의 `canon_updated`에 올렸다. G6 정본 개정 때 함께 본다
- **세션 문서 폴더 구조화** — 지금 평면인데 `workflow.mjs`의 `planDocPath`와 `DocFs.loadSessionDocs`가 둘 다 비재귀라, 폴더로 옮기면 `approve-plan`이 막히고 `정본:` 줄 검사가 조용히 빠진다. 백로그로 빼 두는 쪽을 권했다
- **`FootprintLogic` JSDoc** — 계약에 「Trim 켬」을 적고 긴 망토를 경고하는데 실제 씬은 `trim = false`라 성립하지 않는다. `game/assets/scripts/**`가 `qa-setup`에서 잠겨 못 고쳤다. G5 문서 §3의 3번 단계에 적어 뒀다
- **`PlayerController` 참조 변경과 Player 계층 재구성** — G5(implementation phase)
- **런타임 12항목 검증** — G5 §6.1. 굽기 쪽 4항목은 이미 닫혔다
