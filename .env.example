# fal.ai API 키 — 아트 파이프라인이 매팅 모델을 부를 때 쓴다.
#
# 쓰는 법: 이 파일을 `.env`로 복사한 뒤 값만 채운다. `.env`는 .gitignore가 막으므로
# 커밋되지 않는다. 값은 fal.ai 대시보드(https://fal.ai/dashboard/keys)에서 발급한다.
#
# 셸 환경변수로 두어도 된다 — 도구는 환경변수를 먼저 보고, 없을 때만 이 파일을 읽는다.
# 둘 다 있으면 환경변수가 이긴다(임시로 다른 키를 쓸 때 파일을 안 고쳐도 되게).
FAL_KEY=
